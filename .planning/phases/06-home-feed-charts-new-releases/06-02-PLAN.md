---
phase: 06-home-feed-charts-new-releases
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [backend/app/api/discovery.py, backend/app/main.py, backend/app/core/cache_utils.py, frontend/lib/api/discovery.ts]
autonomous: true
requirements: [HOME-01, HOME-02]

must_haves:
  truths:
    - "/api/home returns personalized home feed"
    - "/api/charts returns trending songs"
    - "/api/explore returns moods and genres"
    - "Endpoints use CacheEntry for database-backed caching"
---

<objective>
Implement the Discovery API endpoints on the backend with regional logic and database-backed caching.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/models/cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Cache Utility</name>
  <files>backend/app/core/cache_utils.py</files>
  <action>
    - Create a helper to get/set JSON data in `CacheEntry` table.
    - Function signature: `async def get_cached_data(db, key: str) -> dict | None`
    - Function signature: `async def set_cached_data(db, key: str, data: dict, ttl_minutes: int)`
  </action>
  <verify>
    <automated>grep "def get_cached_data" backend/app/core/cache_utils.py</automated>
  </verify>
  <done>Cache utility implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Discovery Endpoints</name>
  <files>backend/app/api/discovery.py, backend/app/main.py</files>
  <action>
    - Create `backend/app/api/discovery.py`.
    - Implement `/api/home`: Calls `ytmusicapi.get_home()`. 30m cache.
    - Implement `/api/charts`: Calls `ytmusicapi.get_charts()`. 12h cache.
    - Implement `/api/explore`: Calls `ytmusicapi.get_mood_categories()`. 24h cache.
    - Use `gl` param based on user region if available (or default to 'US').
    - Register router in `main.py`.
  </action>
  <verify>
    <automated>grep "/home" backend/app/api/discovery.py</automated>
  </verify>
  <done>Discovery API implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Frontend Discovery API client</name>
  <files>frontend/lib/api/discovery.ts</files>
  <action>
    - Create `frontend/lib/api/discovery.ts`.
    - Add functions `getHomeFeed()`, `getCharts()`, `getExplore()`.
    - Pass `navigator.language` or similar to backend for regional content if needed.
  </action>
  <verify>
    <automated>grep "getHomeFeed" frontend/lib/api/discovery.ts</automated>
  </verify>
  <done>Frontend API client implemented.</done>
</task>

</tasks>

<verification>
Test API endpoints /api/home, /api/charts, /api/explore return valid JSON.
</verification>

<success_criteria>
Discovery API is functional with caching.
</success_criteria>

<output>
After completion, create `.planning/phases/06-home-feed-charts-new-releases/06-02-SUMMARY.md`
</output>
