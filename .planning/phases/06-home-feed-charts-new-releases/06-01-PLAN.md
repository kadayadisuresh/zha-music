---
phase: 06-home-feed-charts-new-releases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/models/play_history.py, backend/app/models/cache.py, backend/app/models/followed_artist.py, backend/app/models/base_all.py, backend/app/main.py]
autonomous: true
requirements: [HOME-01]

must_haves:
  truths:
    - "Database has a play_history table with required fields"
    - "Database has a cache_entries table for generic API caching"
    - "Database has a followed_artists table"
  artifacts:
    - path: "backend/app/models/play_history.py"
      provides: "PlayHistory SQLAlchemy model"
    - path: "backend/app/models/cache.py"
      provides: "CacheEntry SQLAlchemy model"
---

<objective>
Set up the database schema required for tracking user play history, following artists, and caching discovery API results.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/models/user.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create play_history Model</name>
  <files>backend/app/models/play_history.py</files>
  <action>
    - Create `PlayHistory` model in `backend/app/models/play_history.py`.
    - Fields: id (UUID), user_id (FK), video_id (string 16), title (string 500), artist (string 500), thumbnail_url (text), played_at (timestamptz), play_duration_seconds (int).
  </action>
  <verify>
    <automated>grep "class PlayHistory" backend/app/models/play_history.py</automated>
  </verify>
  <done>PlayHistory model defined.</done>
</task>

<task type="auto">
  <name>Task 2: Create CacheEntry Model</name>
  <files>backend/app/models/cache.py</files>
  <action>
    - Create `CacheEntry` model for database-backed caching of external API calls.
    - Fields: key (string, PK), data (JSON), expires_at (timestamptz), created_at (timestamptz).
  </action>
  <verify>
    <automated>grep "class CacheEntry" backend/app/models/cache.py</automated>
  </verify>
  <done>CacheEntry model defined.</done>
</task>

<task type="auto">
  <name>Task 3: Create followed_artists Model</name>
  <files>backend/app/models/followed_artist.py</files>
  <action>
    - Create `FollowedArtist` model.
    - Fields: user_id (FK), channel_id (string 64), name (string 500), thumbnail_url (text), followed_at (timestamptz).
    - Composite PK: (user_id, channel_id).
  </action>
  <verify>
    <automated>grep "class FollowedArtist" backend/app/models/followed_artist.py</automated>
  </verify>
  <done>FollowedArtist model defined.</done>
</task>

<task type="auto">
  <name>Task 4: Generate and Run Alembic Migration</name>
  <files>backend/alembic/versions/</files>
  <action>
    - Ensure all new models are imported where Alembic can see them (usually in env.py or a base_all.py).
    - Run `alembic revision --autogenerate -m "add_discovery_tables"`
    - Run `alembic upgrade head`
  </action>
  <verify>
    <automated>alembic history | head -n 1</automated>
  </verify>
  <done>Database tables created.</done>
</task>

</tasks>

<verification>
Check database tables exist: play_history, cache_entries, followed_artists.
</verification>

<success_criteria>
Models are defined and tables are created in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/06-home-feed-charts-new-releases/06-01-SUMMARY.md`
</output>
