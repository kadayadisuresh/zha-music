---
phase: 12-jam
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/lib/services/jamService.ts
  - frontend/components/player/JamFooter.tsx
autonomous: false
requirements:
  - SOC-02
user_setup: []
must_haves:
  truths:
    - "UI reflects Jam session host status"
    - "Pulsing indicator shows active Jam"
  artifacts:
    - path: "frontend/lib/services/jamService.ts"
      provides: "Client-side Jam sync service"
    - path: "frontend/components/player/JamFooter.tsx"
      provides: "Jam UI controls"
  key_links:
    - from: "frontend/lib/services/jamService.ts"
      to: "frontend/components/player/JamFooter.tsx"
      via: "Jam context or service hook"
---

<objective>
Build the frontend client-side Jam service and UI components.

Purpose: Implement the Jam session lifecycle, sync logic, and playback controls in the UI.
Output: Jam service, footer UI, and session management buttons.
</objective>

<context>
@.planning/phases/12-jam/12-RESEARCH.md
@frontend/lib/services/
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Jam Service</name>
  <files>frontend/lib/services/jamService.ts</files>
  <action>Create a service or hook to manage WebSocket connection, event handling, and playback synchronization (including clock sync logic with 500ms drift threshold).</action>
  <verify>
    <automated>grep -q "class JamService" frontend/lib/services/jamService.ts</automated>
  </verify>
  <done>Jam service manages socket and playback state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>UI components for Jam Session (Footer, Host Badge, Leave/End buttons).</what-built>
  <how-to-verify>
    1. Join or host a jam session.
    2. Verify footer displays pulsing indicator.
    3. Verify Host Badge appears if host.
    4. Verify Leave/End buttons function.
    5. Test sync behavior (seek and play/pause from host).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client→UI | UI reflects session state; must handle unauthorized state updates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | I | UI | accept | UI is local-only, backend enforces permissions |
</threat_model>

<verification>
Manual verification via checkpoint.
</verification>

<success_criteria>
Jam functionality is integrated into UI and playback syncs correctly.
</success_criteria>

<output>
After completion, create .planning/phases/12-jam/12-03-SUMMARY.md
</output>
