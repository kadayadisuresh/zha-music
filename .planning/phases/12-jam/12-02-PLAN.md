---
phase: 12-jam
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/api/jam.py
  - backend/app/main.py
autonomous: true
requirements:
  - SOC-02
user_setup: []
must_haves:
  truths:
    - "Backend supports WebSocket connections for Jam sessions"
    - "All 9 events are routable via WebSocket manager"
  artifacts:
    - path: "backend/app/api/jam.py"
      provides: "WebSocket jam endpoint and manager"
  key_links:
    - from: "backend/app/api/jam.py"
      to: "backend/app/main.py"
      via: "app.add_websocket_route"
---

<objective>
Implement the core WebSocket orchestration for Jam sessions.

Purpose: Enable real-time synchronization between participants.
Output: Jam WebSocket API endpoints and event routing logic.
</objective>

<context>
@.planning/phases/12-jam/12-RESEARCH.md
@backend/app/api/auth.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create WebSocket Manager</name>
  <files>backend/app/api/jam.py</files>
  <action>Implement JamManager singleton to track active sessions, participants, and event routing. Include all 9 required event handlers (play, pause, seek, next, queue_add, join, leave, sync_state, end).</action>
  <verify>
    <automated>grep -q "class JamManager" backend/app/api/jam.py</automated>
  </verify>
  <done>Manager handles sessions and event routing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire WebSocket endpoints</name>
  <files>backend/app/api/jam.py, backend/app/main.py</files>
  <action>Add WebSocket endpoint `/ws/jam/{join_code}`. Ensure authentication via JWT and session validation before allowing connection.</action>
  <verify>
    <automated>curl -v http://localhost:8000/ws/jam/testcode</automated>
  </verify>
  <done>WebSocket route registered and auth-guarded.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client→WS | WebSocket messages are unauthenticated by default, requires application-layer auth |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | T | WebSocket | mitigate | JWT auth in upgrade header, session validation for every event |
</threat_model>

<verification>
Verify WebSocket endpoint accepts connections after JWT auth.
</verification>

<success_criteria>
WebSocket connections successfully connect to Jam sessions.
</success_criteria>

<output>
After completion, create .planning/phases/12-jam/12-02-SUMMARY.md
</output>
