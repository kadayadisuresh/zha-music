---
phase: 12-jam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/004_add_jam_sessions.py
  - backend/app/models/jam.py
  - backend/app/db/database.py
autonomous: true
requirements:
  - SOC-02
user_setup: []
must_haves:
  truths:
    - "Database tables for Jam sessions and participants exist"
    - "Participant count is constrained to 10"
  artifacts:
    - path: "backend/app/models/jam.py"
      provides: "Jam models"
    - path: "backend/alembic/versions/004_add_jam_sessions.py"
      provides: "Database migration"
  key_links:
    - from: "backend/app/models/jam.py"
      to: "backend/app/db/database.py"
      via: "Base.metadata"
---

<objective>
Implement the Jam session database model and migration.

Purpose: To provide a persistent state for Jam sessions and enforce participant limits.
Output: Jam session and participant database schema.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-jam/12-RESEARCH.md
@backend/app/models/user.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Jam session models</name>
  <files>backend/app/models/jam.py</files>
  <action>Create JamSession and JamParticipant models. JamSession needs host_id, join_code (8-char alphanumeric), and session state. JamParticipant needs user_id, session_id, and role (host/guest).</action>
  <verify>
    <automated>grep -q "class JamSession" backend/app/models/jam.py</automated>
  </verify>
  <done>Models defined, registered with Base.</done>
</task>

<task type="auto">
  <name>Task 2: Create Jam session migration</name>
  <files>backend/alembic/versions/004_add_jam_sessions.py</files>
  <action>Create Alembic migration to add jam_sessions and jam_participants tables. Enforce constraints for join_code and participant limits if possible at DB level, otherwise ensure business logic access.</action>
  <verify>
    <automated>alembic upgrade head</automated>
  </verify>
  <done>Database migrated with new tables.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB→App | Model data is assumed trusted after validation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | I | JamSession | mitigate | Enforce join_code unique and alphanumeric at DB level |
</threat_model>

<verification>
Automated migration check and schema verification.
</verification>

<success_criteria>
Tables jam_sessions and jam_participants exist in test.db.
</success_criteria>

<output>
After completion, create .planning/phases/12-jam/12-01-SUMMARY.md
</output>
