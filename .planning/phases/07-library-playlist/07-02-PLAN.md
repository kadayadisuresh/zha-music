---
phase: 07-library-playlist
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - backend/app/api/library.py
  - backend/app/api/playlist.py
  - backend/app/main.py
autonomous: true
requirements:
  - LIB-01
  - LIB-02
must_haves:
  truths:
    - "API endpoints exist for Likes, Saved Albums, and Playlist CRUD"
    - "Playlist songs can be reordered"
  artifacts:
    - path: "backend/app/api/library.py"
    - path: "backend/app/api/playlist.py"
  key_links:
    - from: "backend/app/api/playlist.py"
      to: "database"
      via: "SQLAlchemy"
---

<objective>
Implement backend API endpoints for library management (Likes, Saved Albums) and Playlists (CRUD + Reordering).

Purpose: Enable library data manipulation.
Output: API endpoints.
</objective>

<context>
# Existing Models: Playlist, PlaylistSong, SavedAlbum, Like
@backend/app/models/playlist.py
@backend/app/models/playlist_song.py
@backend/app/models/saved_album.py
@backend/app/models/like.py
# Existing API: audio.py, auth.py, deps.py
@backend/app/api/deps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Library Endpoints (Likes/Albums)</name>
  <files>backend/app/api/library.py</files>
  <action>
    Create endpoints for:
    - GET /likes, POST /likes, DELETE /likes/{video_id}
    - GET /saved-albums, POST /saved-albums, DELETE /saved-albums/{album_id}
  </action>
  <verify>Endpoints are reachable.</verify>
  <done>Library endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Playlist CRUD & Reorder</name>
  <files>backend/app/api/playlist.py</files>
  <action>
    Create endpoints for:
    - GET/POST/PUT/DELETE /playlists
    - GET/POST/DELETE /playlists/{playlist_id}/songs
    - PUT /playlists/{playlist_id}/reorder (implement midpoint insertion/gap logic)
  </action>
  <verify>Playlist CRUD works, reordering maintains position logic.</verify>
  <done>Playlist endpoints implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → API | Authentication enforced |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Info Disclosure | Playlist | accept | Only owner or shared access can view/edit. |
</threat_model>

<verification>
Automated tests for playlist reordering logic.
</verification>

<success_criteria>
API endpoints functional and secure.
</success_criteria>
