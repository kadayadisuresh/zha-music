---
phase: 07-library-playlist
plan: 04
type: execute
wave: 4
depends_on: [07-02]
files_modified:
  - backend/app/api/storage.py
  - backend/app/core/storage.py
autonomous: true
requirements:
  - LIB-02
must_haves:
  truths:
    - "S3-compatible image upload works"
  artifacts:
    - path: "backend/app/api/storage.py"
  key_links:
    - from: "backend/app/api/storage.py"
      to: "Object Storage"
      via: "S3 SDK"
---

<objective>
Implement S3-compatible image upload for playlist covers.

Purpose: Allow user-customized covers.
Output: Storage API.
</objective>

<context>
@backend/app/api/playlist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Image Storage API</name>
  <files>
    backend/app/api/storage.py,
    backend/app/core/storage.py
  </files>
  <action>
    Create endpoint POST /storage/upload.
    Implement validation: 5MB limit, type (JPG/PNG/WEBP).
    Use aiobotocore or similar to interact with S3/Minio.
  </action>
  <verify>Image uploads store and return a valid URL.</verify>
  <done>Storage API implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → Storage | Image file upload |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Elevation | Storage | mitigate | Validate file types and size before S3 upload. |
</threat_model>

<verification>
Test upload with valid and invalid files.
</verification>

<success_criteria>
Images can be uploaded to S3.
</success_criteria>
