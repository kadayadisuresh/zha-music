---
phase: 07-library-playlist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/playlist.py
  - backend/app/models/playlist_song.py
  - backend/app/models/saved_album.py
  - backend/app/models/like.py
  - backend/app/models/base_all.py
  - backend/alembic/versions/20260527_add_library_models.py
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
must_haves:
  truths:
    - "Library models for Likes, Saved Albums, Playlists, and Playlist Songs exist"
    - "Database tables are correctly migrated"
  artifacts:
    - path: "backend/app/models/playlist.py"
    - path: "backend/app/models/playlist_song.py"
    - path: "backend/app/models/saved_album.py"
    - path: "backend/app/models/like.py"
    - path: "backend/alembic/versions/20260527_add_library_models.py"
  key_links:
    - from: "backend/app/models/base_all.py"
      to: "new library models"
      via: "import and registration"
---

<objective>
Define and migrate the database schema for the Library, Likes, and Playlist systems.

Purpose: Foundation for library features.
Output: SQLAlchemy models and Alembic migration.
</objective>

<context>
# Existing Models: User, PlayHistory, FollowedArtist, CacheEntry
@backend/app/models/user.py
@backend/app/models/base_all.py
@backend/app/models/play_history.py
@backend/app/models/followed_artist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Library Models</name>
  <files>
    backend/app/models/playlist.py,
    backend/app/models/playlist_song.py,
    backend/app/models/saved_album.py,
    backend/app/models/like.py
  </files>
  <action>
    Create models for Playlists (id, user_id, title, description, cover_url, created_at, updated_at), 
    PlaylistSongs (playlist_id, video_id, position, added_at, title, artist, thumbnail_url, duration),
    SavedAlbums (user_id, album_id, title, artist, thumbnail_url, saved_at),
    Likes (user_id, video_id, title, artist, thumbnail_url, duration, liked_at).
    Ensure proper relationships and indexes. 
    PlaylistSongs should use the position-gap strategy (integer).
  </action>
  <verify>Files created and models defined.</verify>
  <done>Models defined.</done>
</task>

<task type="auto">
  <name>Task 2: Register Models & Migrate</name>
  <files>
    backend/app/models/base_all.py,
    backend/alembic/versions/20260527_add_library_models.py
  </files>
  <action>
    Register new models in backend/app/models/base_all.py. 
    Generate and run the Alembic migration.
  </action>
  <verify>Alembic migration runs successfully.</verify>
  <done>Database migrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → DB | Playlist data manipulation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | PlaylistSongs | mitigate | Use server-side validation to ensure user_id owns the playlist before updating order. |
</threat_model>

<verification>
Migration runs cleanly.
</verification>

<success_criteria>
Models imported and tables created.
</success_criteria>
