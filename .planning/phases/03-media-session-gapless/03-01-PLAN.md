---
phase: "03-media-session-gapless"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["frontend/lib/stores/playbackStore.ts", "frontend/lib/audio/AudioEngine.ts"]
autonomous: true
requirements: ["AUDIO-04"]
must_haves:
  truths:
    - "Playback store can hold next track information"
    - "AudioEngine has two separate HTMLAudioElement instances"
  artifacts:
    - path: "frontend/lib/stores/playbackStore.ts"
      provides: "Queue awareness for gapless"
    - path: "frontend/lib/audio/AudioEngine.ts"
      provides: "Dual-player architecture"
  key_links:
    - from: "frontend/lib/audio/AudioEngine.ts"
      to: "frontend/lib/stores/playbackStore.ts"
      via: "setNextTrack and setNetworkType"
---

<objective>
Update the playback store and refactor the AudioEngine to support a dual-player architecture. This is the foundation for gapless playback and pre-fetching.

Purpose: Per D-03, we need two HTMLAudioElement instances to prime the next track while the current one plays.
Output: Refactored AudioEngine with active/inactive player management and updated playback store.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@frontend/lib/audio/AudioEngine.ts
@frontend/lib/stores/playbackStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Playback Store for Queue awareness</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <action>
    Add `nextTrack: Track | null` and `setNextTrack` to the playback store.
    Also add `networkType: string` (defaulting to '4g') and `setNetworkType` to store the connection's effective type.
    This information is needed for the adaptive pre-fetch logic (GAD 1).
  </action>
  <verify>
    <automated>grep -q "nextTrack" frontend/lib/stores/playbackStore.ts && grep -q "networkType" frontend/lib/stores/playbackStore.ts</automated>
  </verify>
  <done>Store supports next track and network type tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AudioEngine for Dual-Buffer Architecture</name>
  <files>frontend/lib/audio/AudioEngine.ts</files>
  <action>
    Refactor AudioEngine to manage an array of two `HTMLAudioElement` instances (`this.players`) instead of a single `this.audio`.
    Maintain an `activeIdx` (0 or 1).
    Update all existing methods (play, pause, seek, setVolume) to target the `activePlayer` (this.players[this.activeIdx]).
    In the constructor, initialize both elements and ensure they have crossOrigin = 'anonymous' for future-proofing.
    Maintain the existing proxy fallback logic for both players.
  </action>
  <verify>
    <automated>grep -q "players: HTMLAudioElement\[\]" frontend/lib/audio/AudioEngine.ts</automated>
  </verify>
  <done>AudioEngine uses a dual-buffer (Ping-Pong) structure per D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Implement prepareNext method</name>
  <files>frontend/lib/audio/AudioEngine.ts</files>
  <action>
    Implement `prepareNext(track: Track)` in AudioEngine.
    This method should:
    1. Determine the `inactivePlayer`.
    2. Resolve the stream URL for the provided track (using existing InnerTube logic).
    3. Set `inactivePlayer.src` to the resolved URL.
    4. Call `inactivePlayer.load()` to prime the buffer.
    5. Ensure `inactivePlayer.pause()` is called to prevent accidental early playback.
    This implements the "Dual-Buffer Architecture" requirement D-03.
  </action>
  <verify>
    <automated>grep -q "async prepareNext" frontend/lib/audio/AudioEngine.ts</automated>
  </verify>
  <done>AudioEngine can pre-load tracks into the inactive buffer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> InnerTube API | Fetching stream URLs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | AudioEngine | mitigate | Ensure stream URLs (which contain auth tokens) are not logged or exposed to other scripts. |
</threat_model>

<success_criteria>
- AudioEngine successfully initializes two audio elements.
- Playback store correctly tracks current and next track.
- `prepareNext` correctly primes the second player without interrupting the first.
</success_criteria>

<output>
After completion, create .planning/phases/03-media-session-gapless/03-01-SUMMARY.md
</output>
