---
phase: 14-downloads-offline
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - frontend/lib/services/downloadService.ts
  - public/download-worker.js
requirements:
  - SYS-01
user_setup: []

must_haves:
  truths:
    - "Downloads can be initiated and tracked"
    - "Worker processes downloads one by one"
  artifacts:
    - path: "frontend/lib/services/downloadService.ts"
      provides: "Download service UI-side orchestration"
      exports: ["downloadTrack", "cancelDownload"]
    - path: "public/download-worker.js"
      provides: "Serial download execution"
  key_links:
    - from: "frontend/lib/services/downloadService.ts"
      to: "public/download-worker.js"
      via: "postMessage"
---

<objective>
Implement the download orchestration service and the actual serial download logic in the worker.
</objective>

<context>
@.planning/phases/14-downloads-offline/14-01-PLAN.md
@frontend/lib/utils/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement download service</name>
  <files>frontend/lib/services/downloadService.ts</files>
  <action>Create `frontend/lib/services/downloadService.ts` to coordinate UI download requests, communicating with the worker via postMessage. Add `downloadTrack` and `cancelDownload` functions (using AbortController).</action>
  <verify>
    <automated>cat frontend/lib/services/downloadService.ts</automated>
  </verify>
  <done>Download service handles track download requests</done>
</task>

<task type="auto">
  <name>Task 2: Implement serial worker logic</name>
  <files>public/download-worker.js</files>
  <action>Implement serial download logic in `public/download-worker.js`. Use AbortController for cancellation and store blobs in IDB via the worker (or relay back to UI). Ensure it processes only one track at a time.</action>
  <verify>
    <automated>cat public/download-worker.js</automated>
  </verify>
  <done>Worker downloads tracks serially and handles cancellation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→Worker | Communicating track URLs to worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | I | Worker | mitigate | Validate blob integrity before saving to IDB. |
</threat_model>

<verification>
Manual verification of download progress and cancellation by initiating a long download and cancelling it mid-process.
</verification>

<success_criteria>
Tracks successfully download to IDB, cancellation works, serial queue maintains order.
</success_criteria>
