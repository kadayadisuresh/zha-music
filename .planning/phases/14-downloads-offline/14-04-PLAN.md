---
phase: 14-downloads-offline
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - frontend/lib/hooks/useStorageCleanup.ts
  - frontend/app/layout.tsx
requirements:
  - SYS-01
user_setup: []

must_haves:
  truths:
    - "Storage estimate works"
    - "Auto-cleanup prompt triggers on startup"
  artifacts:
    - path: "frontend/lib/hooks/useStorageCleanup.ts"
      provides: "Cleanup logic and prompt trigger"
  key_links:
    - from: "frontend/lib/hooks/useStorageCleanup.ts"
      to: "navigator.storage.estimate"
      via: "hook effect"
---

<objective>
Implement the storage cleanup system (estimation, daily prompt, snooze).
</objective>

<context>
@.planning/phases/14-downloads-offline/14-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement storage cleanup hook</name>
  <files>frontend/lib/hooks/useStorageCleanup.ts</files>
  <action>Create `frontend/lib/hooks/useStorageCleanup.ts` that uses `navigator.storage.estimate()` to monitor usage. Implement logic to check if cleanup is needed (daily constraint, 7-day snooze) and trigger a prompt listing items if full/limit reached.</action>
  <verify>
    <automated>test -f frontend/lib/hooks/useStorageCleanup.ts</automated>
  </verify>
  <done>Cleanup hook implements daily check and storage estimation</done>
</task>

<task type="auto">
  <name>Task 2: Integrate cleanup hook</name>
  <files>frontend/app/layout.tsx</files>
  <action>Integrate `useStorageCleanup` in `frontend/app/layout.tsx` so it runs once at application startup.</action>
  <verify>
    <automated>grep -c "useStorageCleanup" frontend/app/layout.tsx</automated>
  </verify>
  <done>Cleanup hook integrated at startup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Storage | Managing browser storage quotas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | I | Cleanup | accept | No user data deletion without explicit confirmation. |
</threat_model>

<verification>
Verify storage estimation log and daily check logic.
</verification>

<success_criteria>
Storage usage is estimated, cleanup trigger occurs appropriately based on limits/snooze settings.
</success_criteria>
