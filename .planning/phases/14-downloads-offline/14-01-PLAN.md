---
phase: 14-downloads-offline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/utils/db.ts
  - frontend/package.json
requirements:
  - SYS-01
user_setup: []

must_haves:
  truths:
    - "IndexedDB stores exist with correct schemas"
    - "Download worker skeleton exists"
  artifacts:
    - path: "frontend/lib/utils/db.ts"
      provides: "IDB connection and stores definition"
      contains: "zha-downloads-meta"
    - path: "public/download-worker.js"
      provides: "Background download worker skeleton"
      contains: "self.addEventListener('message')"
  key_links:
    - from: "frontend/lib/utils/db.ts"
      to: "window.indexedDB"
      via: "idb library"
---

<objective>
Set up the core infrastructure for the download system: IndexedDB dual-store schema and the Web Worker scaffold for serial downloads.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize IDB stores</name>
  <files>frontend/lib/utils/db.ts</files>
  <action>Install `idb` dependency. Create `frontend/lib/utils/db.ts` to define and open `zha-downloads-meta` (stores track/meta info) and `zha-downloads-audio` (stores blobs) stores.</action>
  <verify>
    <automated>npm list idb</automated>
  </verify>
  <done>IDB stores initialized and connected</done>
</task>

<task type="auto">
  <name>Task 2: Create download worker skeleton</name>
  <files>public/download-worker.js</files>
  <action>Create `public/download-worker.js` with basic message handling for triggering a download process. Implement serial logic skeleton.</action>
  <verify>
    <automated>test -f public/download-worker.js</automated>
  </verify>
  <done>Worker file created with message listener</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker→disk | Web Worker saving blobs to IDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | R | IDB | accept | Local storage, no PII. |
| T-14-02 | I | Worker | mitigate | Validate download URLs before processing in worker. |
</threat_model>

<verification>
Automated verification of IDB connection success and worker file existence.
</verification>

<success_criteria>
`idb` is available, IDB stores are initialized on app startup, worker file exists in `public/`.
</success_criteria>
