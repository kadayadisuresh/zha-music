---
phase: 14-downloads-offline
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - frontend/components/ui/OfflineBanner.tsx
  - frontend/components/shared/DownloadBadge.tsx
  - frontend/app/layout.tsx
requirements:
  - SYS-01
user_setup: []

must_haves:
  truths:
    - "Offline banner appears when network is down"
    - "Downloaded items have a badge"
  artifacts:
    - path: "frontend/components/ui/OfflineBanner.tsx"
      provides: "Global offline status banner"
    - path: "frontend/components/shared/DownloadBadge.tsx"
      provides: "Download status badge for tracks"
  key_links:
    - from: "frontend/components/ui/OfflineBanner.tsx"
      to: "navigator.onLine"
      via: "window event listener"
---

<objective>
Implement the UI components for offline indicators (banner and badges).
</objective>

<context>
@.planning/phases/14-downloads-offline/14-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create offline banner</name>
  <files>frontend/components/ui/OfflineBanner.tsx</files>
  <action>Create `frontend/components/ui/OfflineBanner.tsx` which uses `navigator.onLine` and `online`/`offline` window events to show a global warning.</action>
  <verify>
    <automated>test -f frontend/components/ui/OfflineBanner.tsx</automated>
  </verify>
  <done>Banner component created and wired in layout</done>
</task>

<task type="auto">
  <name>Task 2: Create download badge component</name>
  <files>frontend/components/shared/DownloadBadge.tsx</files>
  <action>Create `frontend/components/shared/DownloadBadge.tsx` that checks IDB for existence of audio data and renders a badge if found.</action>
  <verify>
    <automated>test -f frontend/components/shared/DownloadBadge.tsx</automated>
  </verify>
  <done>Badge component created and integrated on track cards</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→IDB | UI querying IDB to check download status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | R | IDB | accept | No PII. |
</threat_model>

<verification>
Offline banner shows/hides correctly on network toggle. Download badges appear after track is downloaded.
</verification>

<success_criteria>
Offline banner and download badges are visible as required.
</success_criteria>
