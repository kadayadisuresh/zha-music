---
phase: 08-radio-autoplay
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [frontend/lib/stores/playbackStore.ts, backend/app/api/audio.py]
autonomous: true
requirements: [PLAY-01, PLAY-02]
must_haves:
  truths:
    - "System fetches radio suggestions using yt.music.getWatchPlaylist"
    - "Duplicates are checked against current session play_history"
    - "Autoplay batches 20 tracks and refreshes at 5 remaining"
    - "Failure retries once, then shows 'Nothing more to play' toast"
  artifacts:
    - path: "backend/app/api/audio.py"
      provides: "Radio suggestions API using ytmusicapi"
    - path: "frontend/lib/stores/playbackStore.ts"
      provides: "Radio state machine and duplicate prevention"
  key_links:
    - from: "frontend/lib/stores/playbackStore.ts"
      to: "/api/radio"
      via: "Radio session management"
---

<objective>
Implement Radio and Autoplay logic including InnerTube integration and smart batching/retries.

Purpose: Completes the core feature requirement for radio and autoplay functionality.
Output: Backend radio API and enhanced `playbackStore.ts` radio state machine.
</objective>

<context>
@frontend/lib/stores/playbackStore.ts
@backend/app/api/audio.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Radio API endpoint</name>
  <files>backend/app/api/audio.py</files>
  <action>
    - Add `/api/radio?videoId={videoId}` endpoint to `audio.py`.
    - Use `ytmusic.get_watch_playlist(videoId)` via `ytmusicapi`.
    - Filter returned tracks against a provided `history` (pass as query param or JSON).
    - Ensure robust error handling with a retry mechanism.
  </action>
  <verify>
    <automated>curl -X GET "/api/radio?videoId=xyz" | grep "tracks"</automated>
  </verify>
  <done>Radio suggestions API functional with duplicate filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement radio state management in store</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <behavior>
    - Test 1: Autoplay refills queue when `queue.length - queueIndex <= 5`.
    - Test 2: Suggestions are added *below* `dividerIndex`.
    - Test 3: Duplicate `videoId` are discarded.
  </behavior>
  <action>
    - Update `fetchAutoplay` in `playbackStore` to use the new Radio API.
    - Add logic to track `play_history` (videoId set) in store.
    - Implement the "refresh at 5 remaining" batching strategy.
    - Add toast notification on failure (retry logic handled by API).
  </action>
  <verify>
    <automated>npm test -- --filter=radioLogic</automated>
  </verify>
  <done>Autoplay state machine manages batching, history, and failures correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Backend | Radio requests containing history array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Denial of Service | Radio API | mitigate | Limit batch sizes and frequency |
</threat_model>

<verification>
Start radio, confirm autoplay appends correctly, check that duplicates don't appear in history.
</verification>

<success_criteria>
Radio and Autoplay function seamlessly with batching and history tracking.
</success_criteria>
