---
phase: 08-radio-autoplay
plan: 03
type: execute
wave: 3
depends_on: [08-01]
files_modified: [frontend/components/queue/QueuePanel.tsx, frontend/components/queue/QueueDivider.tsx]
autonomous: true
requirements: [PLAY-01, PLAY-02]
must_haves:
  truths:
    - "Queue panel displays a visual divider between manual and autoplay sections"
    - "Manual section supports reordering/removal normally"
    - "Autoplay section updates dynamically when tracks are consumed"
  artifacts:
    - path: "frontend/components/queue/QueueDivider.tsx"
      provides: "Visual separator component"
    - path: "frontend/components/queue/QueuePanel.tsx"
      provides: "Queue rendering logic with conditional divider"
  key_links:
    - from: "frontend/components/queue/QueuePanel.tsx"
      to: "frontend/lib/stores/playbackStore.ts"
      via: "dividerIndex consumption"
---

<objective>
Implement the visual Queue Divider UI component and update the Queue panel to render the two-part queue.

Purpose: Completes the user experience requirement for queue separation.
Output: `QueueDivider.tsx` and updated `QueuePanel.tsx`.
</objective>

<context>
@frontend/components/queue/QueuePanel.tsx
@frontend/lib/stores/playbackStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QueueDivider component</name>
  <files>frontend/components/queue/QueueDivider.tsx</files>
  <action>
    - Create a clean `QueueDivider` component that displays a label like "Autoplay Suggestions".
    - Style it to match the application's queue theme.
  </action>
  <verify>
    <automated>ls frontend/components/queue/QueueDivider.tsx</automated>
  </verify>
  <done>QueueDivider UI component created.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate divider in QueuePanel</name>
  <files>frontend/components/queue/QueuePanel.tsx</files>
  <action>
    - Modify `QueuePanel.tsx` to render the `QueueDivider` at `playbackStore.dividerIndex`.
    - Ensure drag-and-drop zones respect the divider (no dragging manual items below the divider, etc.).
    - Update rendering logic to iterate over manual section and autoplay section separately.
  </action>
  <verify>
    <automated>grep -c "QueueDivider" frontend/components/queue/QueuePanel.tsx</automated>
  </verify>
  <done>Queue panel renders a visual separator between manual and autoplay segments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Queue Panel UI | Represents local queue state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Elevation of Privilege | UI rendering | accept | UI only displays data derived from trusted state |
</threat_model>

<verification>
Check that the queue displays the divider in the correct location and renders sections correctly.
</verification>

<success_criteria>
Queue visually communicates the separation of manual vs. autoplay tracks.
</success_criteria>
