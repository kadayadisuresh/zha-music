---
phase: 08-radio-autoplay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/lib/stores/playbackStore.ts]
autonomous: true
requirements: [PLAY-01, PLAY-02]
must_haves:
  truths:
    - "Queue has a visible divider separating manual tracks from autoplay suggestions"
    - "Adding to queue or playing next inserts tracks above the divider"
    - "Removing all manual tracks removes the divider"
    - "Autoplay OFF removes all tracks below the divider"
    - "Shuffle ONLY affects manual tracks, preserving autoplay order"
  artifacts:
    - path: "frontend/lib/stores/playbackStore.ts"
      provides: "Queue divider logic and shuffle separation"
  key_links:
    - from: "frontend/lib/stores/playbackStore.ts"
      to: "frontend/components/queue/QueuePanel.tsx"
      via: "dividerIndex state"
---

<objective>
Refactor the playback store to support a queue divider, ensuring that manual tracks and autoplay suggestions are managed separately.

Purpose: Foundation for GAD-2 (Queue Divider) and GAD-1 (Shuffle/Unshuffle constraints).
Output: Updated `playbackStore.ts` with `dividerIndex`, `addToQueue` logic adjustments, and shuffle separation.
</objective>

<context>
@frontend/lib/stores/playbackStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dividerIndex and update store logic</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <behavior>
    - Test 1: Adding a track to queue inserts it before the divider (if divider exists).
    - Test 2: Removing all manual tracks removes the divider (dividerIndex = -1).
    - Test 3: Shuffle only shuffles `queue.slice(0, dividerIndex)`.
  </behavior>
  <action>
    - Add `dividerIndex` to `PlaybackState`.
    - Update `addToQueue` to insert tracks *above* the `dividerIndex` if it exists.
    - Update `shuffleQueue` to only shuffle items up to `dividerIndex`.
    - Update `removeFromQueue` to handle the divider index shifts.
  </action>
  <verify>
    <automated>npm test -- --filter=playbackStore</automated>
  </verify>
  <done>Queue divider index implemented and managed correctly across store operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store state | Manages the queue structure locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Queue state | accept | Client-side state, user can manipulate own queue |
</threat_model>

<verification>
Verify `dividerIndex` updates correctly when adding tracks, and shuffle only affects the manual section.
</verification>

<success_criteria>
Store logic correctly separates manual tracks from suggestions via `dividerIndex`.
</success_criteria>
