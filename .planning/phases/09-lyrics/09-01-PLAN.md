---
phase: 09-lyrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/lyrics.py
  - backend/app/api/lyrics.py
  - backend/alembic/versions/002_add_lyrics_tables.py
autonomous: true
requirements:
  - PLAY-03
must_haves:
  truths:
    - Backend proxies LRCLIB request with 7-day TTL
    - Sync offset table exists and is accessible
  artifacts:
    - path: backend/app/models/lyrics.py
      provides: Sync offset model
    - path: backend/app/api/lyrics.py
      provides: Proxy endpoint
  key_links:
    - from: backend/app/api/lyrics.py
      to: LRCLIB API
      via: requests
---

<objective>
Implement the backend infrastructure for lyrics, including the database table for sync offsets and the LRCLIB proxy API with caching.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add lyrics models and migrations</name>
  <files>backend/app/models/lyrics.py, backend/alembic/versions/002_add_lyrics_tables.py</files>
  <action>Define LyricsSyncOffset model. Create Alembic migration to add this table.</action>
  <verify>alembic upgrade head</verify>
  <done>Table created in DB.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LRCLIB proxy with cache</name>
  <files>backend/app/api/lyrics.py</files>
  <action>Create endpoint /api/lyrics/{videoId} that checks local DB/cache first, then calls LRCLIB, then caches response (7 days). Ensure correct parsing or data structure passes through.</action>
  <verify>curl -X GET /api/lyrics/videoId (verify response)</verify>
  <done>Proxy endpoint functional.</done>
</task>
</tasks>
