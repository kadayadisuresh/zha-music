---
phase: 09-lyrics
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - frontend/lib/hooks/useLyricsSync.ts
  - backend/app/api/lyrics.py
autonomous: true
requirements:
  - PLAY-03
must_haves:
  truths:
    - Drift detection works
    - Sync offset persists
  artifacts:
    - path: frontend/lib/hooks/useLyricsSync.ts
      provides: Sync/Drift logic
---

<objective>
Implement sync drift detection and persistent track-specific offset adjustment.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement drift detection logic</name>
  <files>frontend/lib/hooks/useLyricsSync.ts</files>
  <action>Implement 5s drift detection check, auto-resync logic (>2s drift), and instant snap (>10s drift).</action>
  <verify>simulated drift test</verify>
  <done>Sync logic operational.</done>
</task>
<task type="auto">
  <name>Task 2: Persistent track offset</name>
  <files>backend/app/api/lyrics.py</files>
  <action>Add endpoint to save/update track sync offset, link to UI slider interaction.</action>
  <verify>check offset table updates</verify>
  <done>Offset saving functional.</done>
</task>
</tasks>
