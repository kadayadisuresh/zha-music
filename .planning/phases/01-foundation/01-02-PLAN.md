---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [backend/app/api/auth.py, backend/app/core/security.py, backend/app/api/deps.py, backend/app/main.py]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: google
    why: "Google OAuth 2.0 PKCE authentication"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs"
      - name: JWT_SECRET
        source: "Generate a random 64-char string"
    dashboard_config:
      - task: "Set Authorized redirect URIs"
        location: "Google Cloud Console -> OAuth Client -> Authorized redirect URIs (e.g., http://localhost:8000/auth/google/callback)"
must_haves:
  truths:
    - "OAuth endpoints correctly format Google redirect URLs"
    - "Valid JWT is issued on successful Google callback"
    - "Session can be cleared via DELETE /auth/session"
  artifacts:
    - path: "backend/app/api/auth.py"
      provides: "Auth API endpoints"
      contains: "/auth/google"
    - path: "backend/app/core/security.py"
      provides: "JWT creation"
      contains: "create_access_token"
  key_links:
    - from: "backend/app/api/auth.py"
      to: "backend/app/models/user.py"
      via: "User lookup and creation"
      pattern: "User"
---

<objective>
Implement Google OAuth 2.0 PKCE flow and JWT session endpoints to authenticate users securely.
Purpose: Allows users to log in without passwords and retrieve their profile data.
Output: Working authentication endpoints in FastAPI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@zha_SRD_v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Google OAuth flow & JWT Issuance</name>
  <files>backend/app/api/auth.py, backend/app/core/security.py, backend/app/main.py</files>
  <action>In `security.py`, add functions to create and verify JWT tokens (100 days expiry). In `auth.py`, implement `GET /auth/google` (redirects to Google consent) and `GET /auth/google/callback` (exchanges code for tokens via `httpx`, creates/updates User in DB, creates JWT, and sets `httpOnly=True, Secure=True, SameSite='Strict'` cookie). Include the auth router in `main.py`.</action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "from app.core.security import create_access_token"</automated>
  </verify>
  <done>OAuth login flow endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement User Session Endpoints</name>
  <files>backend/app/api/auth.py, backend/app/api/deps.py</files>
  <action>Create dependency `get_current_user` in `deps.py` that reads the JWT from cookies, validates it, and fetches the User from the DB. In `auth.py`, add `GET /auth/me` returning the current user schema, and `DELETE /auth/session` which returns a response clearing the JWT cookie.</action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "from app.api.deps import get_current_user"</automated>
  </verify>
  <done>Session info and logout endpoints created and secured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | OAuth callback and JWT issuance |
| Browser -> API | HTTPOnly cookie storage and retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | /auth/google/callback | mitigate | Validate state/nonce from Google response |
| T-01-04 | Information Disclosure | JWT Cookie | mitigate | Set HTTPOnly, Secure, SameSite=Strict on cookie |
| T-01-05 | Elevation of Privilege | /auth/me | mitigate | Validate JWT signature and expiry before returning user data |
</threat_model>

<verification>
Auth routes successfully map to Google OAuth endpoints and issue secure cookies. Session APIs function correctly.
</verification>

<success_criteria>
- `/auth/google` properly redirects.
- JWT issued with 100 days expiration.
- Cookies are strictly configured for security.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>