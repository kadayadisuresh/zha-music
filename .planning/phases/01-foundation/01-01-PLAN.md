---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/requirements.txt, backend/app/main.py, backend/app/core/config.py, backend/app/db/database.py, backend/app/models/user.py, backend/app/models/base.py, backend/alembic.ini, backend/alembic/env.py]
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: postgresql
    why: "Primary database for FastAPI backend"
    env_vars:
      - name: DATABASE_URL
        source: "Local or remote Postgres database (e.g. postgresql+asyncpg://user:pass@localhost:5432/zha)"
must_haves:
  truths:
    - "Backend application starts successfully and connects to database"
    - "Database migrations can be run via Alembic"
    - "Database has a 'users' table matching SRD"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI entry point"
      min_lines: 10
    - path: "backend/app/models/user.py"
      provides: "User ORM model"
      contains: "class User"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/core/config.py"
      via: "startup settings"
      pattern: "settings"
---

<objective>
Initialize the FastAPI backend application and setup the PostgreSQL database models with Alembic for migrations.
Purpose: Provides the foundation for authentication and data storage.
Output: Working FastAPI app and Users table schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@zha_SRD_v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Backend & FastAPI configuration</name>
  <files>backend/requirements.txt, backend/app/main.py, backend/app/core/config.py</files>
  <action>Create `requirements.txt` with `fastapi`, `uvicorn`, `asyncpg`, `sqlalchemy`, `alembic`, `pydantic-settings`, `pyjwt`, `httpx`. Create `config.py` using `pydantic-settings` to load `DATABASE_URL`, `FRONTEND_URL`, `JWT_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`. Setup `main.py` with FastAPI app and CORS middleware allowing credentials and matching `FRONTEND_URL`.</action>
  <verify>
    <automated>cd backend &amp;&amp; pip install -r requirements.txt &amp;&amp; python -c "from app.main import app"</automated>
  </verify>
  <done>FastAPI loads configuration successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Database setup and User model</name>
  <files>backend/app/db/database.py, backend/app/models/user.py, backend/app/models/base.py, backend/alembic.ini, backend/alembic/env.py</files>
  <action>Setup async SQLAlchemy engine and declarative base in `base.py` and `database.py`. Create the `User` model in `user.py` matching the SRD schema (id UUID PK, google_id UNIQUE, email UNIQUE, display_name, avatar_url, crossfade_seconds DEFAULT 0, autoplay_enabled DEFAULT true, created_at, last_seen_at). Initialize Alembic (`alembic init -t async alembic`) and configure `env.py` to run async migrations with the User model.</action>
  <verify>
    <automated>cd backend &amp;&amp; alembic check</automated>
  </verify>
  <done>User model is defined and Alembic is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Backend receives unauthenticated requests during setup phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | DB Connection | mitigate | Use strong credentials and DATABASE_URL env var |
| T-01-02 | Tampering | Alembic Migrations | mitigate | Use strict schema types and validations |
</threat_model>

<verification>
Backend process starts and connects to PostgreSQL. Alembic successfully registers models.
</verification>

<success_criteria>
- FastAPI instance is created and configurable.
- `User` SQLAlchemy model accurately reflects `zha_SRD_v3.md` schema.
- Alembic `env.py` supports async migrations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>