---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: [frontend/app/page.tsx, frontend/app/profile/page.tsx, frontend/components/ui/Button.tsx, frontend/lib/api/client.ts]
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Unauthenticated user sees landing page with login button"
    - "Authenticated user can view their profile data"
    - "Session expiry or 401 error redirects user to landing page silently"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Landing page"
      min_lines: 20
    - path: "frontend/app/profile/page.tsx"
      provides: "User profile view"
      contains: "Profile"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/lib/stores/userStore.ts"
      via: "auth state check"
      pattern: "checkSession"
---

<objective>
Implement the authentication user interface, Google login initiation, and session status handling.
Purpose: Gives users the visual entry point to the application and handles session expiry gracefully.
Output: Functioning Landing Page and Profile Page components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@zha_SRD_v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing Page UI</name>
  <files>frontend/app/page.tsx, frontend/components/ui/Button.tsx</files>
  <action>Create `Button.tsx` reusable UI component. Implement landing page in `page.tsx` for unauthenticated users. Include large centered ഴ logo, tagline 'Your personal YouTube Music. Free. Private. For you and your friends.', and a 'Continue with Google' button (anchors to backend `/auth/google`). On mount, conditionally render based on auth state (redirect to a dummy home feed or show profile if logged in).</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint</automated>
  </verify>
  <done>Landing page UI matches design system and links to OAuth.</done>
</task>

<task type="auto">
  <name>Task 2: Profile Page & Session Expiry Handling</name>
  <files>frontend/app/profile/page.tsx, frontend/lib/api/client.ts</files>
  <action>Implement `/profile` page showing Google avatar, display name, email, and a Logout button. Update `client.ts` to ensure any `401 Unauthorized` API responses silently clear Zustand auth state and redirect to the landing page without a toast.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint</automated>
  </verify>
  <done>Profile page implemented and JWT expiry handling configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Browser | User viewing profile data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | Auth State | mitigate | Verify session against `/auth/me` on mount, don't trust local flags alone |
| T-01-08 | Information Disclosure | Profile Page | mitigate | Display minimal necessary PII on profile |
</threat_model>

<verification>
The application seamlessly routes unauthenticated users to Google, and logged in users can see their profile and logout.
</verification>

<success_criteria>
- Unauthenticated users can log in via Google.
- Authenticated users see profile data.
- Logout clears state and cookie.
- 401 Unauthorized securely redirects to landing page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>