---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/app/layout.tsx, frontend/app/globals.css, frontend/tailwind.config.ts, frontend/lib/api/client.ts, frontend/lib/stores/userStore.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "Next.js application starts and builds successfully"
    - "Zustand store is available and manages user state"
    - "API client is configured to include cookies for CORS"
  artifacts:
    - path: "frontend/app/layout.tsx"
      provides: "Root Next.js layout"
      min_lines: 10
    - path: "frontend/lib/stores/userStore.ts"
      provides: "Zustand user state management"
      contains: "create"
    - path: "frontend/lib/api/client.ts"
      provides: "Centralized fetch client"
      contains: "credentials: 'include'"
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "frontend/app/globals.css"
      via: "CSS imports"
      pattern: "globals.css"
---

<objective>
Initialize the Next.js 15 frontend with Tailwind CSS v4 and set up state management and API client.
Purpose: Provide the UI framework for the YouTube Music clone.
Output: Working Next.js app scaffolding.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@zha_SRD_v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 15 & Tailwind Initialization</name>
  <files>frontend/package.json, frontend/app/layout.tsx, frontend/app/globals.css, frontend/tailwind.config.ts</files>
  <action>Initialize Next.js 15 App Router with Tailwind CSS v4. In `globals.css`, set pure black background and ivory cream text globally (`--bg-primary`, `--accent-primary`). Configure the basic app root layout in `layout.tsx`.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm install &amp;&amp; npm run build</automated>
  </verify>
  <done>Next.js app builds successfully with Tailwind.</done>
</task>

<task type="auto">
  <name>Task 2: API Client and Zustand User Store</name>
  <files>frontend/lib/api/client.ts, frontend/lib/stores/userStore.ts</files>
  <action>Create `client.ts` fetch wrapper with `credentials: 'include'` and `AbortController` support. Create `userStore.ts` using `zustand` to manage `user` state, `checkSession()` (calls `/auth/me`), and `logout()` (calls `DELETE /auth/session`).</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Store and API client are strongly typed and compiled without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Frontend fetching data from backend API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | API Client | mitigate | Ensure CORS credentials: 'include' does not use wildcard origins on backend |
</threat_model>

<verification>
App runs. API client correctly handles credentials. Zustand store correctly defines `user` state.
</verification>

<success_criteria>
- Next.js 15 layout renders successfully.
- API wrapper provides base fetch abstraction.
- userStore exposes `checkSession` and `logout` hooks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>