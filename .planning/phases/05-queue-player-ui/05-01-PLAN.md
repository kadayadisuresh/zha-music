---
phase: 05-queue-player-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/lib/stores/uiStore.ts, frontend/lib/stores/playbackStore.ts, frontend/components/player/ProgressBar.tsx, frontend/components/player/MiniPlayer.tsx, frontend/app/layout.tsx]
autonomous: true
requirements: [PLAYER-01, PLAYER-02]

must_haves:
  truths:
    - "User can see a mini-player at the bottom of the screen when a track is playing"
    - "Mini-player shows track title, artist, and album art"
    - "Mini-player has a 3px scrubbable progress bar at the top"
  artifacts:
    - path: "frontend/components/player/ProgressBar.tsx"
      provides: "Scrubbable playback progress bar"
    - path: "frontend/components/player/MiniPlayer.tsx"
      provides: "Bottom bar player interface"
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "frontend/components/player/MiniPlayer.tsx"
      via: "Component inclusion"
---

<objective>
Establish the core player UI foundation by expanding the stores and implementing the Mini Player (bottom bar).

Purpose: Provides the primary persistent interface for playback control.
Output: Expanded stores, functional ProgressBar, and MiniPlayer component integrated into the layout.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/lib/stores/playbackStore.ts
@frontend/lib/stores/uiStore.ts
@frontend/lib/audio/AudioEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand UI and Playback Stores</name>
  <files>frontend/lib/stores/uiStore.ts, frontend/lib/stores/playbackStore.ts</files>
  <action>
    - Update `uiStore.ts`: Add `isPlayerExpanded` (boolean) and `setPlayerExpanded` action.
    - Update `playbackStore.ts`: Add `repeatMode` ('off' | 'one' | 'all'), `shuffleEnabled` (boolean), and `originalQueue` (Track[]) for unshuffle support.
    - Add `toggleShuffle`, `setRepeatMode` actions.
    - Reference D-01/GAD-01 from 05-RESEARCH.md for shuffle strategy.
  </action>
  <verify>
    <automated>grep -E "isPlayerExpanded|repeatMode|shuffleEnabled" frontend/lib/stores/*.ts</automated>
  </verify>
  <done>Stores expanded with necessary playback and UI states.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ProgressBar component</name>
  <files>frontend/components/player/ProgressBar.tsx</files>
  <action>
    - Create a 3px high progress bar that reflects `currentTime` / `duration` from `playbackStore`.
    - Support scrubbing (click/drag) to call `audioEngine.seek()`.
    - Ensure it is visually subtle but interactive (expand to 5px on hover).
  </action>
  <verify>
    <automated>grep "audioEngine.seek" frontend/components/player/ProgressBar.tsx</automated>
  </verify>
  <done>Scrubbable progress bar implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement MiniPlayer and Integrate into Layout</name>
  <files>frontend/components/player/MiniPlayer.tsx, frontend/app/layout.tsx</files>
  <action>
    - Build `MiniPlayer.tsx` containing: `ProgressBar` at top, album art (small), metadata (title/artist), Play/Pause, and Next button.
    - Clicking the player background (excluding controls) should set `isPlayerExpanded` to true.
    - Mount `MiniPlayer` in `frontend/app/layout.tsx` at the bottom of the screen, only visible if `currentTrack` is not null.
    - Use `framer-motion` for entry/exit animation of the bar.
  </action>
  <verify>
    <automated>grep "MiniPlayer" frontend/app/layout.tsx</automated>
  </verify>
  <done>MiniPlayer visible and functional at the bottom of the app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Store | Interaction updates state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ProgressBar | mitigate | Validate seek time is within 0 and duration. |
</threat_model>

<verification>
Verify MiniPlayer appears when music plays and seek works.
</verification>

<success_criteria>
MiniPlayer is functional and persistent across page navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-queue-player-ui/05-01-SUMMARY.md`
</output>
