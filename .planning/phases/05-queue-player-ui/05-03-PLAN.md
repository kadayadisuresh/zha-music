---
phase: 05-queue-player-ui
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [frontend/lib/stores/playbackStore.ts, frontend/components/queue/QueueList.tsx, frontend/components/queue/QueueItem.tsx]
autonomous: true
requirements: [PLAYER-01]

must_haves:
  truths:
    - "User can view the current playback queue"
    - "User can drag and drop tracks to reorder the queue"
    - "User can shuffle the queue and then unshuffle to restore original order"
    - "User can remove individual tracks or clear the entire queue"
  artifacts:
    - path: "frontend/components/queue/QueueList.tsx"
      provides: "Sortable queue list container"
    - path: "frontend/components/queue/QueueItem.tsx"
      provides: "Draggable queue item"
  key_links:
    - from: "frontend/components/queue/QueueList.tsx"
      to: "@dnd-kit/sortable"
      via: "DND integration"
---

<objective>
Implement the sophisticated queue management system with drag-and-drop capabilities and unshuffle logic.

Purpose: Empowers users to curate their immediate listening experience.
Output: Functional QueueList and QueueItem components with full mutation logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-queue-player-ui/05-01-SUMMARY.md
@frontend/lib/stores/playbackStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Advanced Queue Logic in Store</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <action>
    - Implement `reorderQueue(oldIndex, newIndex)` using standard array splice.
    - Implement `removeFromQueue(index)` and `clearQueue()`.
    - Implement `shuffleQueue()` (using Fisher-Yates, per RESEARCH.md) and `unshuffleQueue()` (restoring from `originalQueue`).
    - Implement `addNext(track)` (insert after current index) and `addToQueue(track)` (append to end).
    - Ensure `queueIndex` stays in sync during mutations.
  </action>
  <verify>
    <automated>grep -E "reorderQueue|shuffleQueue|unshuffleQueue" frontend/lib/stores/playbackStore.ts</automated>
  </verify>
  <done>Queue mutation logic fully implemented in Zustand.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sortable Queue UI</name>
  <files>frontend/components/queue/QueueList.tsx, frontend/components/queue/QueueItem.tsx</files>
  <action>
    - Install `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` if not present.
    - Build `QueueList.tsx` using `DndContext` and `SortableContext`.
    - Build `QueueItem.tsx` with `useSortable` hook.
    - Include track metadata, "remove" button, and drag handle in `QueueItem`.
    - Highlight the currently playing track in the list.
    - Use `react-window` for virtualization if the queue is large (optional but recommended for performance).
  </action>
  <verify>
    <automated>grep "useSortable" frontend/components/queue/QueueItem.tsx</automated>
  </verify>
  <done>Drag-to-reorder queue UI implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement LocalStorage Persistence</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <action>
    - Use Zustand `persist` middleware to save `volume`, `repeatMode`, `shuffleEnabled`, and `isMuted` to `localStorage`.
    - Ensure `volume` is restored on initial load.
    - Ensure hydration is handled correctly to avoid SSR mismatch.
  </action>
  <verify>
    <automated>grep "persist" frontend/lib/stores/playbackStore.ts</automated>
  </verify>
  <done>User playback preferences persist across sessions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Store | Reordering updates state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | queue | mitigate | Validate queue indices are within bounds during reorder/remove. |
</threat_model>

<verification>
Add tracks to queue, reorder them via drag, shuffle/unshuffle, and verify volume persists after refresh.
</verification>

<success_criteria>
Queue management is robust, performant, and persistent.
</success_criteria>

<output>
After completion, create `.planning/phases/05-queue-player-ui/05-03-SUMMARY.md`
</output>
