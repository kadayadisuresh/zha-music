---
phase: 05-queue-player-ui
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [frontend/components/player/FullPlayer.tsx, frontend/components/player/PlayerControls.tsx, frontend/app/layout.tsx]
autonomous: true
requirements: [PLAYER-03]

must_haves:
  truths:
    - "User can expand Mini Player into a full screen/sidebar player"
    - "Desktop: 380px sidebar appears on the right"
    - "Mobile: Fullscreen overlay appears with swipe-to-dismiss"
    - "Background adapts to the currently playing track's artwork"
  artifacts:
    - path: "frontend/components/player/FullPlayer.tsx"
      provides: "Expanded player interface"
  key_links:
    - from: "frontend/components/player/FullPlayer.tsx"
      to: "frontend/components/ui/AdaptiveBackground.tsx"
      via: "Usage for player background"
---

<objective>
Implement the expanded "Now Playing" interface for both desktop and mobile, ensuring smooth animations and adaptive aesthetics.

Purpose: Provides the immersive playback experience with full controls.
Output: FullPlayer component with responsive layout and gesture support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-queue-player-ui/05-01-SUMMARY.md
@frontend/components/ui/AdaptiveBackground.tsx
@frontend/lib/hooks/useAdaptiveColor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Shared Player Controls</name>
  <files>frontend/components/player/PlayerControls.tsx</files>
  <action>
    - Extract playback buttons into a reusable component: Like, Shuffle, Prev, Play/Pause, Next, Repeat, Autoplay toggle, Sleep Timer, Volume.
    - Wire them to `playbackStore` actions.
    - Style them to be accessible and match the YTM aesthetic.
  </action>
  <verify>
    <automated>grep -E "Like|Shuffle|Prev|Play|Next|Repeat" frontend/components/player/PlayerControls.tsx</automated>
  </verify>
  <done>Reusable player controls implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FullPlayer with Desktop Sidebar</name>
  <files>frontend/components/player/FullPlayer.tsx, frontend/app/layout.tsx</files>
  <action>
    - Build `FullPlayer.tsx` using `framer-motion` for transitions.
    - Use `layoutId="player-thumb"` for the album art to animate from `MiniPlayer`.
    - Desktop: Render as a fixed 380px sidebar on the right side.
    - Wire visibility to `isPlayerExpanded` from `uiStore`.
    - Integrate `PlayerControls` and metadata.
    - Ensure `AdaptiveBackground` logic is applied to the player surface.
  </action>
  <verify>
    <automated>grep "380px" frontend/components/player/FullPlayer.tsx</automated>
  </verify>
  <done>Desktop expanded player implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Mobile Fullscreen Overlay with Gestures</name>
  <files>frontend/components/player/FullPlayer.tsx</files>
  <action>
    - Mobile: Render as a full-screen overlay (fixed inset-0).
    - Implement "swipe down to dismiss" using `framer-motion` `drag="y"` and `onDragEnd` logic from 05-RESEARCH.md.
    - Implement "swipe left on album art" placeholder for lyrics view (per user requirement).
    - Ensure smooth backdrop blur and entry/exit animations.
  </action>
  <verify>
    <automated>grep "drag=\"y\"" frontend/components/player/FullPlayer.tsx</automated>
  </verify>
  <done>Mobile gesture-based player interface implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Store | Interaction updates state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Denial of Service | FullPlayer | mitigate | Ensure heavy animations/filters are disabled if low performance detected. |
</threat_model>

<verification>
Check expansion animation on both desktop (sidebar) and mobile (fullscreen). Verify gestures.
</verification>

<success_criteria>
FullPlayer provides a high-fidelity, interactive experience on all devices.
</success_criteria>

<output>
After completion, create `.planning/phases/05-queue-player-ui/05-02-SUMMARY.md`
</output>
