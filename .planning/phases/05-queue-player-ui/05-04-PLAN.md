---
phase: 05-queue-player-ui
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified: [frontend/lib/audio/AudioEngine.ts, frontend/lib/stores/playbackStore.ts, frontend/components/browse/TrackList.tsx, frontend/components/shared/TrackContextMenu.tsx]
autonomous: true
requirements: [PLAY-02, PLAYER-01]

must_haves:
  truths:
    - "Playback correctly loops the current track when 'Repeat One' is active"
    - "Autoplay fetches and appends suggestions when reaching the end of the user queue"
    - "User can right-click/long-press tracks to access 'Play Next' and 'Add to Queue'"
  artifacts:
    - path: "frontend/components/shared/TrackContextMenu.tsx"
      provides: "Context menu for track actions"
  key_links:
    - from: "frontend/lib/audio/AudioEngine.ts"
      to: "frontend/lib/stores/playbackStore.ts"
      via: "Repeat mode checking"
    - from: "frontend/lib/stores/playbackStore.ts"
      to: "/api/innertube/upnext"
      via: "Autoplay fetching"
---

<objective>
Finalize the playback logic with "Repeat One" support and integrate Autoplay suggestions from InnerTube.

Purpose: Ensures music continuity and provides deep interaction patterns.
Output: Updated AudioEngine, Autoplay logic, and Track Context Menus.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-queue-player-ui/05-03-SUMMARY.md
@frontend/lib/audio/AudioEngine.ts
@frontend/lib/stores/playbackStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AudioEngine for Repeat One</name>
  <files>frontend/lib/audio/AudioEngine.ts</files>
  <action>
    - Modify `switchTracks` or `ended` listener in `AudioEngine.ts`.
    - If `repeatMode === 'one'`, instead of switching to `nextTrack`, seek to 0 and call `play()`.
    - Ensure Media Session controls reflect the loop state if possible.
  </action>
  <verify>
    <automated>grep "repeatMode" frontend/lib/audio/AudioEngine.ts</automated>
  </verify>
  <done>Repeat One logic implemented in the audio engine.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Autoplay (getUpNext) Integration</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <action>
    - Add `fetchAutoplay()` action to `playbackStore.ts`.
    - It should call `/api/innertube/upnext?video_id={current_id}` via the proxy/backend.
    - Append returned tracks to the queue under an "Autoplay" section header (or just append if simpler).
    - Trigger `fetchAutoplay()` when the user starts playing the *last* track in the manually added queue (per GAD-02).
  </action>
  <verify>
    <automated>grep "fetchAutoplay" frontend/lib/stores/playbackStore.ts</automated>
  </verify>
  <done>Autoplay suggestions seamlessly integrated into the queue.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Track Context Menus</name>
  <files>frontend/components/shared/TrackContextMenu.tsx, frontend/components/browse/TrackList.tsx</files>
  <action>
    - Create `TrackContextMenu.tsx` with options: "Play Next", "Add to Queue", "Go to Artist", "Go to Album".
    - Wire "Play Next" and "Add to Queue" to `playbackStore` actions.
    - Integrate context menu trigger (right-click or three-dot menu) into `TrackList.tsx` items.
    - Support mobile long-press or dedicated menu button.
  </action>
  <verify>
    <automated>grep "Play Next" frontend/components/shared/TrackContextMenu.tsx</automated>
  </verify>
  <done>Track interaction context menus implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> Store | Suggestions fetched from API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Autoplay | accept | Suggestions are public YouTube content. |
</threat_model>

<verification>
Enable Repeat One and verify track loops. Play the last track in a queue and verify autoplay tracks appear. Test context menu actions.
</verification>

<success_criteria>
Playback logic is complete and highly interactive.
</success_criteria>

<output>
After completion, create `.planning/phases/05-queue-player-ui/05-04-SUMMARY.md`
</output>
