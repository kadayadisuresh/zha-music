---
phase: 04-search-browse
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified: [frontend/lib/stores/searchStore.ts, frontend/components/search/SearchBar.tsx, frontend/app/search/page.tsx, frontend/components/search/SearchSkeleton.tsx]
autonomous: true
requirements: [SEARCH-01]

must_haves:
  truths:
    - "Search bar debounces input by 300ms before fetching"
    - "Recent searches are persisted and shown when focused/empty"
    - "Search results show categorized shelves (Songs, Albums, Artists)"
    - "Skeleton loaders appear during fetching"
  artifacts:
    - path: "frontend/lib/stores/searchStore.ts"
      provides: "Search history state"
    - path: "frontend/components/search/SearchBar.tsx"
      provides: "Debounced input UI"
---

<objective>
Build the search interface featuring instant results, recent history, and optimized loading states.

Purpose: Provide a fast, interactive discovery experience.
Output: Search components and page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@frontend/lib/api/mappers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Search Bar and History Store</name>
  <files>frontend/lib/stores/searchStore.ts, frontend/components/search/SearchBar.tsx</files>
  <action>
    1. Create `frontend/lib/stores/searchStore.ts` using Zustand:
       - State for `recentSearches: string[]`.
       - Persist to localStorage using `persist` middleware.
       - Methods: `addSearch(query)`, `clearHistory()`.
    2. Create `frontend/components/search/SearchBar.tsx`:
       - Use `use-debounce` (300ms) for the search query. (Per D-01)
       - On focus + empty value: show Recent Searches from the store. (Per D-01)
       - Loading state logic:
         - Show a spinner inside the search bar if `isLoading` is true for > 2s (Per D-01).
       - Navigation: Update URL query param `?q=...` as user types (debounced).
  </action>
  <verify>
    <automated>npm run lint --prefix frontend</automated>
  </verify>
  <done>Search bar correctly debounces, persists history, and handles slow loading states.</done>
</task>

<task type="auto">
  <name>Task 2: Build Search Results Page and Skeletons</name>
  <files>frontend/app/search/page.tsx, frontend/components/search/SearchSkeleton.tsx</files>
  <action>
    1. Create `frontend/components/search/SearchSkeleton.tsx`:
       - Provides skeleton layouts for categorized shelves. (Per D-01)
    2. Create `frontend/app/search/page.tsx`:
       - Reads `q` from search params.
       - Fetches data from `/api/search?q=...`.
       - Displays categorized sections: "Top Result" (optional), "Songs", "Albums", "Artists".
       - Uses Skeletons during fetching state. (Per D-01)
       - Uses `TrackItem`, `AlbumCard`, `ArtistCircle` components (create if needed or use basic placeholders).
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Search page displays results in shelves with skeletons during transitions.</done>
</task>

</tasks>

<verification>
Manual verification: Type in search bar, check 300ms debounce, check recent searches appear on focus, check shelves render correctly.
</verification>

<success_criteria>
Instant search works with debouncing. Recent searches persist. Results are categorized.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-browse/04-03-SUMMARY.md`
</output>
