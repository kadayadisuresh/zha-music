---
phase: 04-search-browse
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-04]
files_modified: [frontend/lib/hooks/useAdaptiveColor.ts, frontend/components/ui/AdaptiveBackground.tsx, frontend/app/layout.tsx]
autonomous: true
requirements: [SEARCH-02]

must_haves:
  truths:
    - "Background color updates when viewing different content (Artist/Album)"
    - "Transitions between background colors take exactly 500ms"
  artifacts:
    - path: "frontend/lib/hooks/useAdaptiveColor.ts"
      provides: "Header extraction logic"
    - path: "frontend/components/ui/AdaptiveBackground.tsx"
      provides: "Animated background layer"
---

<objective>
Implement the adaptive background UI that changes based on the dominant color of the currently viewed artwork.

Purpose: Enhance the visual immersion of the application.
Output: Adaptive color hook and background component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@frontend/app/api/proxy/image/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAdaptiveColor hook</name>
  <files>frontend/lib/hooks/useAdaptiveColor.ts</files>
  <action>
    1. Create `frontend/lib/hooks/useAdaptiveColor.ts`:
       - Accepts an `imageUrl`.
       - If `imageUrl` is provided, it constructs the proxy URL: `/api/proxy/image?url=${encodeURIComponent(imageUrl)}`.
       - Performs a `HEAD` request to the proxy URL to extract the `X-Dominant-Color` header. (Per D-02)
       - Falls back to a default color if extraction fails.
       - Returns the color string.
  </action>
  <verify>
    <automated>npm run lint --prefix frontend</automated>
  </verify>
  <done>Hook correctly extracts the dominant color header from the proxy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AdaptiveBackground and Integrate in Layout</name>
  <files>frontend/components/ui/AdaptiveBackground.tsx, frontend/app/layout.tsx</files>
  <action>
    1. Create `frontend/components/ui/AdaptiveBackground.tsx`:
       - A fixed-position component that sits behind other content.
       - Accepts a `color` prop.
       - Uses CSS transitions (or Framer Motion) to animate the `backgroundColor` with a duration of 500ms. (Per D-02)
    2. Update `frontend/app/layout.tsx`:
       - Place `AdaptiveBackground` at the root of the application UI.
       - Listen to route changes or page-specific state to update the current dominant color.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Global background color smoothly transitions when navigating between content pages.</done>
</task>

</tasks>

<verification>
Manual verification: Open an album, check if background changes color. Open another album, check for 500ms transition.
</verification>

<success_criteria>
Background color adapts to artwork with smooth 500ms transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-browse/04-05-SUMMARY.md`
</output>
