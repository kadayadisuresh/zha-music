---
phase: 04-search-browse
plan: 04
type: execute
wave: 2
depends_on: [04-02]
files_modified: [frontend/app/artist/[id]/page.tsx, frontend/app/album/[id]/page.tsx, frontend/components/browse/TrackList.tsx]
autonomous: true
requirements: [SEARCH-02]

must_haves:
  truths:
    - "Artist page shows artist details, top songs, and discography"
    - "Album page shows album artwork, metadata, and full tracklist"
  artifacts:
    - path: "frontend/app/artist/[id]/page.tsx"
      provides: "Artist details view"
    - path: "frontend/app/album/[id]/page.tsx"
      provides: "Album details view"
---

<objective>
Implement detail pages for Artists and Albums to allow deep browsing of music content.

Purpose: Provide full access to discography and tracklists.
Output: Artist and Album pages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@frontend/lib/api/mappers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Artist Detail Page</name>
  <files>frontend/app/artist/[id]/page.tsx</files>
  <action>
    1. Create `frontend/app/artist/[id]/page.tsx`:
       - Fetches data from `/api/browse/artist/[id]`.
       - Renders a header with artist name and banner/image.
       - Renders a "Top Songs" section (typically a list of 5-10 tracks).
       - Renders discography sections (Albums, Singles, etc.) using carousels or grids. (Per D-04)
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Artist page displays name, top songs, and categorized discography correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Build Album Detail Page and TrackList component</name>
  <files>frontend/app/album/[id]/page.tsx, frontend/components/browse/TrackList.tsx</files>
  <action>
    1. Create `frontend/components/browse/TrackList.tsx`:
       - A reusable component to render a list of tracks with index, title, artist, and duration.
       - Supports "Play" action (integrate with `AudioEngine` if possible, otherwise use a placeholder).
    2. Create `frontend/app/album/[id]/page.tsx`:
       - Fetches data from `/api/browse/album/[id]`.
       - Renders album cover, title, artist (linked), and release info.
       - Renders the full `TrackList`. (Per D-04)
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Album page displays artwork and full tracklist correctly.</done>
</task>

</tasks>

<verification>
Manual verification: Navigate to an artist from search, check sections. Navigate to an album, check tracklist.
</verification>

<success_criteria>
Artist and Album pages are fully functional and correctly display metadata and music content.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-browse/04-04-SUMMARY.md`
</output>
