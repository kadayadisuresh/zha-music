---
phase: 04-search-browse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/lib/api/mappers.ts, frontend/app/api/search/route.ts, frontend/app/api/search/suggestions/route.ts]
autonomous: true
requirements: [SEARCH-01]

must_haves:
  truths:
    - "Search API returns categorized results (Songs, Albums, Artists) mapped to internal types"
    - "Suggestions API returns a list of strings for autocomplete"
  artifacts:
    - path: "frontend/lib/api/mappers.ts"
      provides: "Zod schemas and YT node mappers"
    - path: "frontend/app/api/search/route.ts"
      provides: "Unified search endpoint"
  key_links:
    - from: "frontend/app/api/search/route.ts"
      to: "youtubei.js"
      via: "yt.music.search"
---

<objective>
Establish the data contracts for music entities using Zod and implement the search/suggestions endpoints.

Purpose: Ensure the frontend receives clean, typed data from InnerTube and supports instant search features.
Output: Mapped data structures and functional search API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-search-browse/04-RESEARCH.md
@frontend/lib/innertube/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup dependencies and define data contracts</name>
  <files>frontend/package.json, frontend/lib/api/mappers.ts</files>
  <action>
    1. Install `zod`, `colorthief`, and `use-debounce` in the frontend directory.
    2. Create `frontend/lib/api/mappers.ts`.
    3. Define Zod schemas for `Track`, `Album`, `Artist`, and `SearchResult` (unified).
    4. Implement mapping functions that take `youtubei.js` types (e.g., `MusicResponsiveListItem`) and return Zod-validated internal types.
    5. Handle thumbnail selection: pick the highest resolution available.
    (Per D-05)
  </action>
  <verify>
    <automated>npm run lint --prefix frontend</automated>
  </verify>
  <done>Zod schemas defined and mappers correctly transform YT nodes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Search and Suggestions API routes</name>
  <files>frontend/app/api/search/route.ts, frontend/app/api/search/suggestions/route.ts</files>
  <action>
    1. Create `frontend/app/api/search/route.ts`:
       - Accepts `q` (query) and `type` (optional filter).
       - Calls `yt.music.search(q)`.
       - Maps the results using mappers from Task 1.
       - Returns categorized shelves: `songs`, `albums`, `artists`. (Per D-03)
    2. Create `frontend/app/api/search/suggestions/route.ts`:
       - Accepts `q` query.
       - Calls `yt.music.getSearchSuggestions(q)`.
       - Returns a flat list of strings.
  </action>
  <verify>
    <automated>curl -G "http://localhost:3000/api/search" --data-urlencode "q=Never Gonna Give You Up"</automated>
  </verify>
  <done>Endpoints return valid JSON results from InnerTube.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Next.js API | Untrusted search queries cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Search API | mitigate | Sanitize query inputs, validate types with Zod |
| T-04-02 | Denial of Service | Search API | mitigate | Implement basic rate limiting or rely on InnerTube's own protection; front-end debouncing (Wave 2) |
</threat_model>

<verification>
Verify search results structure matches the internal types and contains items from multiple categories.
</verification>

<success_criteria>
Unified search returns mapped Songs, Albums, and Artists. Suggestions return matching strings.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-browse/04-01-SUMMARY.md`
</output>
