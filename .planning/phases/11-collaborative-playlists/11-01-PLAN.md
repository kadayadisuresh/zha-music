---
phase: 11-collaborative-playlists
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/playlist.py
  - backend/alembic/versions/003_add_collaborative_playlist_fields.py
autonomous: true
requirements:
  - SOC-01
user_setup: []
must_haves:
  truths:
    - "Playlist table updated to support collaboration"
    - "Playlist songs table updated to support versioning"
  artifacts:
    - path: "backend/app/models/playlist.py"
      provides: "Playlist and PlaylistSong model updates"
    - path: "backend/alembic/versions/003_add_collaborative_playlist_fields.py"
      provides: "Migration for new database fields"
---

<objective>
Update the database schema to support collaborative playlists, including collaboration toggles, invite tokens, and song versioning for conflict resolution.
</objective>

<context>
@backend/app/models/playlist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Playlist and PlaylistSong models</name>
  <files>backend/app/models/playlist.py</files>
  <action>
    Update Playlist model to add `is_collaborative` (Boolean), `invite_token` (String, indexed), `invite_token_expires_at` (DateTime).
    Update PlaylistSong model to add `version` (Integer, default=1).
  </action>
  <verify>
    <automated>python -c "from app.models.playlist import Playlist; assert hasattr(Playlist, 'is_collaborative')"</automated>
  </verify>
  <done>Models updated with required fields for collaborative functionality.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Alembic migration</name>
  <files>backend/alembic/versions/003_add_collaborative_playlist_fields.py</files>
  <action>
    Generate and apply a new Alembic migration to reflect the changes to `Playlist` and `PlaylistSong` models in the database.
  </action>
  <verify>
    <automated>alembic upgrade head</automated>
  </verify>
  <done>Database schema migrated to support collaborative playlists.</done>
</task>

</tasks>

<verification>
  Ensure `playlists` table has `is_collaborative`, `invite_token`, `invite_token_expires_at` columns, and `playlist_songs` has `version` column.
</verification>

<success_criteria>
  Schema changes applied successfully to the database.
</success_criteria>
