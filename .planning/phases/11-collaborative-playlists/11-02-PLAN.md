---
phase: 11-collaborative-playlists
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/app/api/playlist.py
autonomous: true
requirements:
  - SOC-01
user_setup: []
must_haves:
  truths:
    - "Collaborative playlists can be enabled/disabled via API"
    - "Invite tokens can be generated and managed"
  artifacts:
    - path: "backend/app/api/playlist.py"
      provides: "New endpoints for collaborative toggle and invite management"
---

<objective>
Implement API endpoints for managing collaborative playlist state and invite tokens.
</objective>

<context>
@backend/app/api/playlist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collaborative toggle and invite generation endpoints</name>
  <files>backend/app/api/playlist.py</files>
  <action>
    Add POST /playlists/{id}/collaborative to toggle collaboration.
    Add POST /playlists/{id}/invite to generate/regenerate an invite token (32-char) and set expiration.
    Add GET /playlists/join/{token} to validate the token and return the playlist ID.
  </action>
  <verify>
    <automated># Placeholder for integration test verification</automated>
  </verify>
  <done>Collaborative management and invite system endpoints implemented.</done>
</task>

</tasks>

<verification>
  Verify endpoints correctly update database fields and handle token generation/validation.
</verification>

<success_criteria>
  API endpoints support enabling collaboration and generating/verifying invite tokens.
</success_criteria>
