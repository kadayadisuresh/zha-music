---
phase: 16-pwa-polish
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [frontend/components/pwa/InstallPrompt.tsx, frontend/components/pwa/PWAUpdateToast.tsx, frontend/app/offline/page.tsx, frontend/app/layout.tsx]
autonomous: false
requirements: [SYS-02]
---

<objective>
Implement the PWA user experience, including installation prompts, non-intrusive updates, and an offline fallback page.

Purpose: Improve user retention and provide a smooth offline experience.
Output: Install prompt, Update toast, and Offline fallback page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-pwa-polish/16-01-SUMMARY.md
@frontend/app/layout.tsx
@frontend/lib/stores/playerStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PWA Install Prompt</name>
  <files>frontend/components/pwa/InstallPrompt.tsx</files>
  <action>
    Create `InstallPrompt.tsx` client component.
    - Listen for `beforeinstallprompt` event.
    - Implement a 2-minute delay (`setTimeout`) before showing the prompt banner.
    - Persist dismissal state in `localStorage` (`pwa_prompt_dismissed`).
    - Use brand colors (Black bg, Ivory Cream accents).
  </action>
  <verify>
    <automated>grep "beforeinstallprompt" frontend/components/pwa/InstallPrompt.tsx && grep "120000" frontend/components/pwa/InstallPrompt.tsx</automated>
  </verify>
  <done>Install prompt component implemented with delay and persistence logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Background Update Toast (GAD 1)</name>
  <files>frontend/components/pwa/PWAUpdateToast.tsx</files>
  <action>
    Create `PWAUpdateToast.tsx` client component using `@serwist/next` hooks (if available) or standard SW update events.
    - Display "Update now" toast at the top of the screen when a new SW is waiting.
    - Logic: Trigger `skipWaiting` and `window.location.reload()` ONLY after the current song ends (check `playerStore.isPlaying`) or if the app has been idle (no song in queue).
    - If a song is playing, wait for the `ended` state before reloading.
  </action>
  <verify>
    <automated>grep "Update now" frontend/components/pwa/PWAUpdateToast.tsx</automated>
  </verify>
  <done>Update toast implemented with playback-aware reload logic.</done>
</task>

<task type="auto">
  <name>Task 3: Create Offline Fallback Page (GAD 2)</name>
  <files>frontend/app/offline/page.tsx, frontend/app/layout.tsx</files>
  <action>
    Create `frontend/app/offline/page.tsx`.
    - Content: "You are offline. Redirecting to your downloads in 2s..."
    - Implement `useEffect` countdown that redirects to `/downloads` after 2 seconds.
    - Handle zero-downloads case (display a message if IndexedDB is empty).
    Register `PWAUpdateToast` and `InstallPrompt` in `frontend/app/layout.tsx`.
  </action>
  <verify>
    <automated>test -f frontend/app/offline/page.tsx</automated>
  </verify>
  <done>Offline page created with redirect logic; PWA components integrated into RootLayout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PWA UI components (Install Prompt, Update Toast, Offline Page)</what-built>
  <how-to-verify>
    1. Start the app and wait 2 minutes to see the install prompt.
    2. Dismiss it and verify it doesn't reappear on refresh.
    3. Simulate offline mode in DevTools and navigate to a non-cached route (e.g., /search/non-existent).
    4. Verify the redirect to /downloads occurs.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → Browser | User interacts with install/update prompts. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Denial of Service | Update Toast | mitigate | Ensure update logic doesn't infinite-loop reloads if SW activation fails. |
</threat_model>

<verification>
Manual verification of the install prompt and offline redirect logic.
</verification>

<success_criteria>
Install prompt shows after delay. Offline redirect works as specified. Updates are playback-aware.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pwa-polish/16-02-SUMMARY.md`
</output>
