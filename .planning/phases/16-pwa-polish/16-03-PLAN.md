---
phase: 16-pwa-polish
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified: [frontend/components/ui/Skeleton.tsx, frontend/components/search/SearchPage.tsx, frontend/components/layout/AppBackground.tsx, frontend/app/globals.css]
autonomous: true
requirements: [SYS-02]
---

<objective>
Enhance the application's accessibility, responsive design, and visual feedback using skeleton loaders.

Purpose: Provide a polished, professional feel and ensure the app is usable by everyone.
Output: Skeleton component, A11y improvements, and Responsive fixes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/app/globals.css
@frontend/components/layout/layout.tsx
@frontend/lib/stores/playerStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Skeleton Loaders</name>
  <files>frontend/components/ui/Skeleton.tsx, frontend/components/search/SearchPage.tsx</files>
  <action>
    Create a generic `Skeleton.tsx` component with a subtle pulse animation.
    Implement skeleton states for:
    - Search results (grid of album/artist cards).
    - Artist detail header and top tracks.
    - Home feed horizontal rows.
    Ensure skeletons match the layout of the actual content to prevent layout shift (CLS).
  </action>
  <verify>
    <automated>grep "animate-pulse" frontend/components/ui/Skeleton.tsx</automated>
  </verify>
  <done>Skeleton loaders implemented for major data-fetching views.</done>
</task>

<task type="auto">
  <name>Task 2: Accessibility (A11y) Implementation</name>
  <files>frontend/components/player/MiniPlayer.tsx, frontend/components/player/FullPlayer.tsx</files>
  <action>
    Audit and add missing `aria-label` attributes to all buttons (Play, Pause, Shuffle, etc.).
    Implement `aria-live="polite"` for song changes in the player to notify screen readers of the new track title.
    Ensure focus rings are visible and logical tab order is maintained throughout the app.
    Check contrast ratios for Ivory Cream #F5F0E8 on Black #000000 (should be > 4.5:1).
  </action>
  <verify>
    <automated>grep "aria-label" frontend/components/player/MiniPlayer.tsx && grep "aria-live" frontend/components/player/FullPlayer.tsx</automated>
  </verify>
  <done>A11y enhancements complete including labels and live regions.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive Polish</name>
  <files>frontend/app/globals.css, frontend/components/layout/Navbar.tsx</files>
  <action>
    Review layout across breakpoints: 375px (Mobile S), 768px (Tablet), 1024px (Laptop), 1440px (Desktop).
    Fix any overflow issues or broken grids.
    Ensure the MiniPlayer and Queue are perfectly positioned on mobile devices.
    Optimize font sizes and spacing for smaller screens.
  </action>
  <verify>
    <automated>grep "@media" frontend/app/globals.css</automated>
  </verify>
  <done>Responsive audit and fixes applied across all targeted device widths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → User | Visual/auditory feedback to the user. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information Disclosure | Skeletons | accept | Skeletons do not reveal data; they only show the shape of expected data. |
</threat_model>

<verification>
Manual visual check of skeletons and responsive behavior.
</verification>

<success_criteria>
Skeletons visible during loading. Accessibility labels present. Responsive layout is stable.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pwa-polish/16-03-SUMMARY.md`
</output>
