---
phase: 16-pwa-polish
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified: [frontend/.env.example, backend/.env.example, README.md, frontend/package.json]
autonomous: true
requirements: [SYS-02]
---

<objective>
Finalize the project with comprehensive documentation, environment examples, and a clean build.

Purpose: Ensure the project is ready for deployment and easy for other developers to set up.
Output: .env.example files, README.md, and a clean production build.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/package.json
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Environment and Documentation</name>
  <files>frontend/.env.example, backend/.env.example, README.md</files>
  <action>
    Create `frontend/.env.example` with all required Next.js public and private variables.
    Create `backend/.env.example` with database URLs and API keys.
    Create a comprehensive root `README.md` covering:
    - Project overview.
    - Local setup instructions (backend & frontend).
    - Environment variable descriptions.
    - Deployment guide (specifically targeting Oracle Cloud as requested).
    - Features list (including PWA, Downloads, etc.).
  </action>
  <verify>
    <automated>test -f README.md && test -f frontend/.env.example && test -f backend/.env.example</automated>
  </verify>
  <done>Documentation and environment templates finalized.</done>
</task>

<task type="auto">
  <name>Task 2: Production Build & Performance Check</name>
  <files>frontend/package.json</files>
  <action>
    Run `npm run build` in the frontend directory.
    Fix any TypeScript errors, linting warnings, or build issues.
    Verify that `public/sw.js` and `public/manifest.webmanifest` are generated.
    Manually run a Lighthouse audit in a production-like environment (if possible) to ensure:
    - PWA score >= 90
    - Performance score >= 80
    - FCP < 2s, LCP < 3s, CLS < 0.1
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Clean production build achieved with no TS errors. Performance targets verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Build System → Artifacts | Build process transforms source code into production assets. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | README/.env.example | mitigate | Do NOT include real secrets in .env.example or README.md. |
</threat_model>

<verification>
Verify build success and existence of .env.example files.
</verification>

<success_criteria>
Documentation is complete. Build is error-free. Performance metrics are met.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pwa-polish/16-04-SUMMARY.md`
</output>
