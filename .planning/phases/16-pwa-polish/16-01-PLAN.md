---
phase: 16-pwa-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/next.config.ts, frontend/app/manifest.ts, frontend/app/sw.ts]
autonomous: true
requirements: [SYS-02]
must_haves:
  truths:
    - "The application serves a valid web app manifest at /manifest.webmanifest"
    - "A service worker is registered and active in production builds"
    - "API requests to Search (5m TTL) and Artist (1h TTL) are cached by the service worker"
  artifacts:
    - path: "frontend/app/manifest.ts"
      provides: "Dynamic PWA manifest"
    - path: "frontend/app/sw.ts"
      provides: "Service Worker source with Serwist"
    - path: "frontend/next.config.ts"
      provides: "Serwist integration"
  key_links:
    - from: "frontend/next.config.ts"
      to: "frontend/app/sw.ts"
      via: "withSerwistInit"
---

<objective>
Setup the core PWA infrastructure using Serwist and Next.js native manifest. This forms the foundation for installability and offline caching (GAD 3).

Purpose: Enable PWA capabilities and runtime caching.
Output: Configured Serwist, functional manifest, and base service worker.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/16-pwa-polish/16-RESEARCH.md
@frontend/package.json
@frontend/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PWA dependencies and wrap config</name>
  <files>frontend/package.json, frontend/next.config.ts</files>
  <action>
    Install `@serwist/next` and `serwist`. 
    Update `next.config.ts` to use `withSerwistInit` from `@serwist/next`.
    Configure it to point to `app/sw.ts` as the source and `sw.js` as the destination in public.
    Ensure `disable: process.env.NODE_ENV === 'development'` is set to avoid caching issues during dev.
  </action>
  <verify>
    <automated>cd frontend && npm list @serwist/next serwist</automated>
  </verify>
  <done>Dependencies installed and next.config.ts wrapped.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Native Manifest</name>
  <files>frontend/app/manifest.ts</files>
  <action>
    Create `frontend/app/manifest.ts` using Next.js `MetadataRoute.Manifest` type.
    Set:
    - background_color: #000000
    - theme_color: #000000
    - icons: icon-192.png, icon-512.png (both regular and maskable).
    - display: standalone
    - name: "ZHA Better"
    - short_name: "ZHA"
    Reference Ivory Cream #F5F0E8 for any character-based icon descriptions if applicable.
  </action>
  <verify>
    <automated>test -f frontend/app/manifest.ts</automated>
  </verify>
  <done>Manifest file created with required brand colors and metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Create Service Worker with Runtime Caching</name>
  <files>frontend/app/sw.ts</files>
  <action>
    Create `frontend/app/sw.ts` using `installSerwist`.
    Implement `runtimeCaching` rules:
    - `/api/search`: NetworkFirst, 5-minute TTL (maxAgeSeconds: 300).
    - `/api/artist`: NetworkFirst, 1-hour TTL (maxAgeSeconds: 3600).
    - Exclude audio/user data from caching (matchers for `/api/proxy/audio` and `/api/auth/me`).
    Use `defaultCache` from `@serwist/next/browser` as a base.
  </action>
  <verify>
    <automated>grep "maxAgeSeconds: 300" frontend/app/sw.ts && grep "maxAgeSeconds: 3600" frontend/app/sw.ts</automated>
  </verify>
  <done>Service worker source implemented with specific TTL rules for Search and Artist APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → SW | SW intercepts all requests; potential for cache poisoning if SW script is compromised. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | sw.ts | mitigate | Use standard Serwist plugins and ensure SW is served over HTTPS (enforced by browser). |
| T-16-02 | Information Disclosure | Cache | mitigate | Explicitly exclude sensitive endpoints (/api/auth/me, /api/user/*) from runtime caching. |
</threat_model>

<verification>
Run `npm run build` in frontend to ensure SW and manifest are generated correctly.
</verification>

<success_criteria>
Valid manifest and SW source committed. Build succeeds with SW output.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pwa-polish/16-01-SUMMARY.md`
</output>
