---
phase: 02-audio-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/lib/innertube/session.ts, frontend/app/api/innertube/stream/route.ts]
autonomous: true
requirements: [AUDIO-02]

must_haves:
  truths:
    - "InnerTube session is initialized on the server"
    - "PO Token is generated and cached on the backend"
    - "API returns a valid YouTube stream URL for a given video ID"
  artifacts:
    - path: "frontend/lib/innertube/session.ts"
      provides: "Singleton InnerTube session manager"
    - path: "frontend/app/api/innertube/stream/route.ts"
      provides: "GET /api/innertube/stream endpoint"
  key_links:
    - from: "frontend/app/api/innertube/stream/route.ts"
      to: "frontend/lib/innertube/session.ts"
      via: "getStreamUrl call"
---

<objective>
Initialize the InnerTube API using youtubei.js and implement the PO Token management logic.

Purpose: To provide a reliable way to fetch YouTube stream URLs from the backend, handling the required Proof of Origin (PO) tokens and caching to avoid rate limits and 403 errors.
Output: A working Next.js API route that resolves video IDs to stream URLs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install youtubei.js and implement InnerTube session manager</name>
  <files>frontend/package.json, frontend/lib/innertube/session.ts</files>
  <action>
    1. Install `youtubei.js@^17.0.0` in the frontend directory.
    2. Create `frontend/lib/innertube/session.ts` to manage a singleton InnerTube instance.
    3. Implement a `getInnertube()` function that returns the cached instance or initializes a new one using `Innertube.create()`.
    4. Per GAD 2, implement a caching mechanism for the session and PO Token. Since this is server-side, use a global variable (for dev HMR safety) or a persistent cache if needed.
  </action>
  <verify>
    <automated>cd frontend && npm list youtubei.js</automated>
  </verify>
  <done>youtubei.js is installed and the session manager is scaffolded.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PO Token background refresh</name>
  <files>frontend/lib/innertube/session.ts</files>
  <action>
    1. Implement a background refresh loop that periodically updates the PO Token before it expires.
    2. The session should use the generated PO Token for all requests.
    3. Ensure the token is shared across all users by using the singleton session.
    4. Reference: Use `session.generatePoToken()` or similar youtubei.js v17 patterns.
  </action>
  <verify>
    <automated>Check session.ts for setInterval or background task logic for token refresh</automated>
  </verify>
  <done>PO Token logic is implemented and shared.</done>
</task>

<task type="auto">
  <name>Task 3: Create Stream API Route</name>
  <files>frontend/app/api/innertube/stream/route.ts</files>
  <action>
    1. Create a Next.js API route `GET /api/innertube/stream?videoId=...`.
    2. Extract `videoId` from query params.
    3. Use the InnerTube session to get info for the video (`innertube.getInfo(videoId)`).
    4. Choose the best audio-only format (typically M4A or OPUS).
    5. Return the stream URL and expiration timestamp in the JSON response.
  </action>
  <verify>
    <automated>curl "http://localhost:3000/api/innertube/stream?videoId=dQw4w9WgXcQ"</automated>
  </verify>
  <done>API route returns a stream URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Next.js API | External requests for stream URLs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | /api/innertube/stream | mitigate | Validate videoId format to prevent injection/abuse |
| T-02-02 | Denial of Service | InnerTube Session | mitigate | Rate limit stream URL requests to prevent YouTube IP bans |
</threat_model>

<verification>
Verify that calling the API route returns a valid URL starting with `https://rr` (YouTube CDN).
</verification>

<success_criteria>
Next.js API can successfully provide playable YouTube stream URLs with valid PO tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-engine/02-01-SUMMARY.md`
</output>
