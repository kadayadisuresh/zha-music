---
phase: 02-audio-engine
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [frontend/lib/stores/playbackStore.ts, frontend/lib/audio/AudioEngine.ts, frontend/app/page.tsx]
autonomous: true
requirements: [AUDIO-01, GAD-1]

must_haves:
  truths:
    - "AudioEngine is a singleton and safe for HMR"
    - "Playback state (playing, volume, progress) is managed by Zustand"
    - "Engine automatically falls back to proxy on 403 errors"
    - "Basic play/pause/seek controls work in the browser"
  artifacts:
    - path: "frontend/lib/stores/playbackStore.ts"
      provides: "Zustand store for playback state"
    - path: "frontend/lib/audio/AudioEngine.ts"
      provides: "Singleton AudioEngine class"
  key_links:
    - from: "frontend/lib/audio/AudioEngine.ts"
      to: "frontend/lib/stores/playbackStore.ts"
      via: "state synchronization"
    - from: "frontend/lib/audio/AudioEngine.ts"
      to: "backend/api/audio/proxy"
      via: "fallback URL construction"
---

<objective>
Implement the frontend playback engine and global state management for audio.

Purpose: To provide a robust, state-driven audio player that handles YouTube streaming and fallback logic.
Output: A working AudioEngine singleton and a Zustand store connected to a basic UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@frontend/lib/stores/userStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Playback Store</name>
  <files>frontend/lib/stores/playbackStore.ts</files>
  <action>
    1. Create `frontend/lib/stores/playbackStore.ts` using Zustand.
    2. Store state: `currentTrack` (id, title, artist, cover), `isPlaying`, `duration`, `currentTime`, `volume`, `isMuted`, `isLoading`.
    3. Implement actions: `setPlaying(bool)`, `setProgress(time)`, `setVolume(vol)`, `setTrack(track)`.
  </action>
  <verify>
    <automated>Check for create() and export usePlaybackStore in playbackStore.ts</automated>
  </verify>
  <done>Zustand store for playback is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HMR-safe Singleton AudioEngine</name>
  <files>frontend/lib/audio/AudioEngine.ts</files>
  <action>
    1. Create `frontend/lib/audio/AudioEngine.ts` containing an `AudioEngine` class.
    2. The class should wrap a native `HTMLAudioElement`.
    3. Use the `globalThis` pattern to ensure the singleton survives HMR in development.
    4. Implement methods: `play(videoId)`, `pause()`, `seek(time)`, `setVolume(val)`.
    5. Sync `HTMLAudioElement` events (`play`, `pause`, `timeupdate`, `loadedmetadata`, `error`) with the `usePlaybackStore`.
  </action>
  <verify>
    <automated>Check for globalThis assignment and HTMLAudioElement events in AudioEngine.ts</automated>
  </verify>
  <done>AudioEngine singleton is implemented and HMR-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Fallback Logic and Test UI</name>
  <files>frontend/lib/audio/AudioEngine.ts, frontend/app/page.tsx</files>
  <action>
    1. In `AudioEngine.play(videoId)`, first call `/api/innertube/stream` to get the direct URL.
    2. Set `audio.src` to the direct URL.
    3. Listen for the `error` event on the audio element. If a 403 error occurs, construct the proxy URL (`/api/audio/proxy/{videoId}`) and set `audio.src` to the proxy.
    4. In `frontend/app/page.tsx`, add a simple test interface: an input for `videoId`, and buttons for Play, Pause, and a Seek bar.
    5. Connect the UI to `usePlaybackStore` and `AudioEngine`.
  </action>
  <verify>
    <automated>Manual verification: Enter a video ID and check if audio plays and seeks.</automated>
  </verify>
  <done>Playback logic with proxy fallback and test UI is functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| AudioEngine -> Browser Audio Element | Direct control of playback |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Playback Store | accept | Store only transient playback state; no sensitive user data |
</threat_model>

<verification>
Open the app, enter a YouTube video ID, and verify that audio plays, can be paused/resumed, and seeking works correctly.
</verification>

<success_criteria>
User can play audio from YouTube with automatic proxy fallback and state-synced UI controls.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-engine/02-03-SUMMARY.md`
</output>
