---
phase: 02-audio-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [backend/app/api/audio.py, backend/app/main.py]
autonomous: true
requirements: [AUDIO-02, GAD-1]

must_haves:
  truths:
    - "Proxy handles 403 errors by streaming audio through the backend"
    - "Proxy supports seeking via HTTP Range requests"
    - "Proxy does not download the whole file before streaming (chunked)"
  artifacts:
    - path: "backend/app/api/audio.py"
      provides: "FastAPI /api/audio/proxy route"
  key_links:
    - from: "backend/app/api/audio.py"
      to: "frontend/api/innertube/stream"
      via: "internal HTTP call to resolve videoId"
---

<objective>
Implement a backend audio proxy in FastAPI to serve as a fallback when the browser encounters 403 Forbidden errors from the YouTube CDN.

Purpose: To ensure playback reliability even when YouTube's direct-to-browser streaming is blocked.
Output: A streaming endpoint in FastAPI that supports range requests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Audio Proxy Route</name>
  <files>backend/app/api/audio.py, backend/app/main.py</files>
  <action>
    1. Create `backend/app/api/audio.py` and register it in `backend/app/main.py`.
    2. Implement `GET /api/audio/proxy/{video_id}`.
    3. Inside the route, first call the Next.js internal API (e.g., `http://localhost:3000/api/innertube/stream?videoId={video_id}`) to get a fresh stream URL.
    4. Per GAD 1, implement chunked streaming using `httpx.AsyncClient().stream()`.
    5. Ensure the proxy supports HTTP Range requests by forwarding the `Range` header from the client to YouTube and returning the appropriate headers (`Content-Range`, `Content-Length`, `Accept-Ranges`).
  </action>
  <verify>
    <automated>curl -v -H "Range: bytes=0-1023" "http://localhost:8000/api/audio/proxy/dQw4w9WgXcQ"</automated>
  </verify>
  <done>Proxy route is implemented and supports range requests.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Proxy for Performance</name>
  <files>backend/app/api/audio.py</files>
  <action>
    1. Use a singleton `httpx.AsyncClient` for proxying to reuse connections.
    2. Set appropriate timeouts and handle connection errors gracefully.
    3. Ensure `StreamingResponse` is used to pipe the data directly to the client with minimal memory overhead.
  </action>
  <verify>
    <automated>Check for StreamingResponse and AsyncClient usage in audio.py</automated>
  </verify>
  <done>Proxy is performant and memory-efficient.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> FastAPI Proxy | External requests for audio content |
| FastAPI -> YouTube CDN | Backend requests for content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Audio Proxy | mitigate | Enforce authentication and rate limits on the proxy route |
| T-02-04 | Spoofing | Stream URL Resolution | mitigate | Ensure the internal call to Next.js is secure or uses a shared secret |
</threat_model>

<verification>
Verify that the proxy can serve a small chunk of audio data with a 206 status code using `curl`.
</verification>

<success_criteria>
FastAPI proxy successfully streams audio from YouTube with full seeking support (Range headers).
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-engine/02-02-SUMMARY.md`
</output>
