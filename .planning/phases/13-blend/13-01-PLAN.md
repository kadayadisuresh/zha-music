---
phase: 13-blend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/models/blend.py, backend/app/models/base_all.py, backend/alembic/versions/005_add_blend_tables.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Blend database models exist"
    - "Database migration added blend tables"
  artifacts:
    - path: "backend/app/models/blend.py"
      provides: "Blend, BlendInvite models"
    - path: "backend/alembic/versions/005_add_blend_tables.py"
      provides: "Alembic migration"
  key_links:
    - from: "backend/app/models/blend.py"
      to: "backend/app/models/base_all.py"
      via: "Base inclusion"
---

<objective>
Define database schema for Blend functionality.

Purpose: To support the storage of Blend relationships, invites, and daily playlist data.
Output: New SQLAlchemy models and database migration.
</objective>

<context>
@backend/app/models/base_all.py
@backend/app/models/playlist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Blend and BlendInvite models</name>
  <files>backend/app/models/blend.py, backend/app/models/base_all.py</files>
  <action>Create Blend and BlendInvite models in blend.py. BlendInvite should have user_id relationships and status fields. Include Blend model with user relationships. Register models in base_all.py.</action>
  <verify>
    <automated>grep -c "class Blend" backend/app/models/blend.py</automated>
  </verify>
  <done>Models defined and imported in base_all.py</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for Blend tables</name>
  <files>backend/alembic/versions/005_add_blend_tables.py</files>
  <action>Use alembic to generate a migration creating the blend_invites and blends tables as defined in the new models.</action>
  <verify>
    <automated>alembic upgrade head</automated>
  </verify>
  <done>Migration created and applied to database</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→API} | {Blend invite actions} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | blend_invites | mitigate | {validate user relationships, enforce status transitions} |
| T-13-02 | Info Disclosure | blend_invites | accept | {data visible to invited users only} |
</threat_model>

<verification>
Check that migration succeeded and models are available in the app.
</verification>

<success_criteria>
Database tables `blends` and `blend_invites` exist with correct foreign key constraints.
</success_criteria>
