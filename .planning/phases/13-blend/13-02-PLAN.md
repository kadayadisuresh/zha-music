---
phase: 13-blend
plan: 02
type: execute
wave: 2
depends_on: [13-01-PLAN.md]
files_modified: [backend/app/api/blend.py, backend/app/main.py, backend/app/core/scheduler.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Blend API endpoints exist"
    - "Daily batch job configured for Blend update"
  artifacts:
    - path: "backend/app/api/blend.py"
      provides: "Endpoints for blend invites and blend data"
    - path: "backend/app/core/scheduler.py"
      provides: "Batch job registration"
  key_links:
    - from: "backend/app/api/blend.py"
      to: "backend/app/models/blend.py"
      via: "Database operations"
---

<objective>
Implement Blend API and backend scheduler tasks.

Purpose: To enable invitation management and the daily playlist generation process.
Output: API endpoints and APScheduler job.
</objective>

<context>
@backend/app/api/playlist.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Blend API endpoints</name>
  <files>backend/app/api/blend.py, backend/app/main.py</files>
  <action>Implement endpoints: SEARCH user, SEND invite, ACCEPT/DECLINE invite, GET blend info. Add router to main.py.</action>
  <verify>
    <automated># Verify endpoints are mounted</automated>
  </verify>
  <done>Endpoints functional</done>
</task>

<task type="auto">
  <name>Task 2: Implement daily batch scheduler</name>
  <files>backend/app/core/scheduler.py</files>
  <action>Add a daily APScheduler job at 00:00 UTC to recompute blends. Add logic to skip if unused for 30 days.</action>
  <verify>
    <automated># Check scheduler logs</automated>
  </verify>
  <done>Batch job registered and running daily</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→API} | {Blend API authorization} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Authorization | Blend API | mitigate | {verify current_user is party to the blend} |
</threat_model>

<verification>
Verify API endpoints respond correctly and scheduler is registered.
</verification>

<success_criteria>
Users can invite each other, and daily task triggers blend updates.
</success_criteria>
